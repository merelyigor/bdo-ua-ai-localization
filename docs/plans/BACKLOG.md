# Черга робіт

Єдине місце для всього залишку роботи: великі теми посилаються на окремі плани,
дрібні задачі можуть жити без окремого файла.

- **Статуси:** `готово` · `в роботі` · `чекає` · `відкладено` · `перевірити`.
- Рядок має бути самодостатнім для нової сесії: ціль, шлях до плану й блокер.
- Статус оновлюється разом із кодом або документацією, які його доводять.

## Правила, безпека і документація

| Статус | Задача | Де деталі |
|---|---|---|
| `готово` | Правила, механічні gates, публічну документацію та структуру `bdo-ua-ai-localization` актуалізовано за серверним проєктом і шаблонами. Local API smoke лишився зовнішньою перевіркою через HTTP 401. | [done/2026-08-21_PUBLIC_REPOSITORY_RULES_AND_DOCUMENTATION.md](done/2026-08-21_PUBLIC_REPOSITORY_RULES_AND_DOCUMENTATION.md) |

## Якість перекладу

| Статус | Задача | Де деталі |
|---|---|---|
| `готово` | Нерозпізнані назви (`probable_unresolved`) тепер у payload воркера і QA. Заодно виправлено хибне твердження, що такі рядки йдуть у модерацію: за замовчуванням вони ЗАПИСУЮТЬСЯ в ШІ-шар (`cli/batch/batch-commit.sh:136` вимагає `--names-to-moderation`). | [backlog/2026-08-22_TRANSLATION_QUALITY_IMPROVEMENTS.md](backlog/2026-08-22_TRANSLATION_QUALITY_IMPROVEMENTS.md), Q1 |
| `перевірити` | Суперечність про квадратні теґи прибрано в пʼяти місцях: правда тепер у `keep`, який справді перевіряє `Row::tokenViolations()`. Лишилось: додати `Title` -> `Титул` у глосарій (адмінка, `manage_glossary`) і звірити на живій пачці. | той самий план, Q2 |
| `готово` | `examples` тепер доходять і до QA: контекст зберігається в `context.json` теки пачки замість тимчасового файла, нуль додаткових викликів API. | той самий план, Q4 |
| `готово` | `examples` увімкнені за замовчуванням із трьома запобіжниками: проба перших 3 рядків, повторне використання `context.json`, деградація без прикладів при недоступному API. `--no-context` вимикає. | той самий план, Q3 |
| `готово` | Розмір пачки: дефолт 15 (рішення власника 2026-08-22), 10 для якості й 20 для швидкості. Змінено в промпті, `bdo help flow`, README і `docs/API.md`. Лишилось звірити частку не-PASS при N=15 на живому прогоні. | той самий план, Q5 |
| `готово` | Діагностика `./bdo bench`: замість одного рядка «не JSON-масив» тепер видно content/reasoning/finish_reason/токени, уривок відповіді й підказку за симптомом; сира відповідь зберігається у `output/benchmark/`. | той самий план, S1 сусідній |
| `перевірити` | `translation-terminology` більше не читає пачку: вхід · `./bdo payload terminology` з готовим resolve, `read` прибрано з інструментів. Лишилось зміряти вхідні токени на живому прогоні · без чинного ключа `resolve` дає `no_answer`. | той самий план, S1 |
| `відкладено` | 86% рядків прод-прогону в ручному шарі дослівно збіглись із ШІ-перекладом. Три варіанти від 2026-08-17, вибір за власником. | той самий план, останній розділ |

## Архітектура й відказостійкість

| Статус | Задача | Де деталі |
|---|---|---|
| `в роботі` | Економіка флоу: пачка з 50 рядків коштує чотирьох майже повних проходів моделі (worker 50, QA 50, repair 37, контрольний QA 37). Пʼять змін · механіка перед QA, канарка на 3-5 рядках, QA завжди повертає готовий текст, контрольний QA злитий із суддею, журнал пише кожен дитячий виклик. Контрольні метрики «до» зафіксовані в плані. | [active/2026-08-27_FLOW_AND_SUBAGENT_ECONOMY.md](active/2026-08-27_FLOW_AND_SUBAGENT_ECONOMY.md) |
| `в роботі` | 70-80% child-сесій ідуть на локальних моделях (рішення власника 2026-08-27), тому child-промпти проєктуються під `ollama-local/qwen3.6:35b-a3b-mtp-q4_K_M`, а мовний зрив ловиться детектором, не проханням у промпті. Зроблено: `ForeignScript` (ієрогліфи, кана, хангиль) у механічних дефектах, заборона чужої писемності в промптах worker і repair, розділ «Доступно на покращення ШІ», пункти `1в`/`1г`, `tests/primary-prompt-drift.sh`, еталон у нормативі §8.12. Далі: журнал дефектів промпта, вимір якості child окремо для локальної моделі, build-time генерація спільного блоку. | [active/2026-08-27_LOCAL_MODEL_PROMPT_HARDENING.md](active/2026-08-27_LOCAL_MODEL_PROMPT_HARDENING.md) |
| `чекає` | Розбір напрямку проєкту за вимірами 2026-08-26: ШІ-шар закритий на 97,9% (лишилось 29 936 рядків), 18% рядків пачки гинуть у `state/quarantine.jsonl` з `api_source_equivalent` і цим же спричиняють `no_progress`, диригент витрачає ~10 700 вхідних токенів платної моделі на рядок проти ~110 у воркера, а `improve` не має сигналу якості. | [backlog/2026-08-26_PROJECT_DIRECTION.md](backlog/2026-08-26_PROJECT_DIRECTION.md) |
| `перевірити` | Кросплатформенність через WSL-міст: native Windows OpenCode виконує вже дозволену команду як `wsl.exe --cd <root> bash -lc "<команда>"`. Реалізовано 2026-08-25 разом із `.gitattributes`, резолвером даних OpenCode за межею WSL і `sqlite3` у preflight. Лишилось підтвердити на живій Windows-машині · з macOS цю частину перевірити неможливо. | [active/2026-08-25_CROSS_PLATFORM_WINDOWS_BRIDGE.md](active/2026-08-25_CROSS_PLATFORM_WINDOWS_BRIDGE.md) |
| `відкладено` | Порт оркестрації в `php bdo.php` (єдина залежність PHP 8.3, native Windows без WSL). Заміряно 2026-08-25: 44 з 49 `cli/**/*.sh` уже делегують у PHP. Не починати, доки WSL-міст покриває потребу. | той самий план, шлях 3 |
| `готово` | Windows/WSL2 та provider-neutral маршрути субагентів: policy за ролями, явна вартість, fallback і фактичний route у receipt. | [done/2026-08-22_WSL2_AND_MODEL_ROUTING.md](done/2026-08-22_WSL2_AND_MODEL_ROUTING.md) |
| `готово` | Автономна система OpenCode має чотири українські primary-режими, справжні дочірні worker/QA/repair сесії, відновлювану машину станів, retry та idempotent API write. PROD acceptance записав 1/1 рядок у `machine` і завершив manifest як `verified`. | [done/2026-08-22_UNATTENDED_OPENCODE_TRANSLATION_SYSTEM.md](done/2026-08-22_UNATTENDED_OPENCODE_TRANSLATION_SYSTEM.md) |
| `готово` | Порядок пачки й відновлення живуть у `./bdo run drive`; root має 0 `*.sh`, реалізацію розкладено в 10 категорій `cli/**`. | [done/2026-08-21_PIPELINE_ENGINE_AND_REPO_STRUCTURE.md](done/2026-08-21_PIPELINE_ENGINE_AND_REPO_STRUCTURE.md) |
| `готово` | Єдиний вхід `./bdo` з деревом команд і `./bdo help flow`; перевірка посилань у gate розширена з `AGENTS.md` на всі документи й промпти; биті посилання виправлені. | той самий план, етапи 0 і 1 |
| `готово` | Ціль прогону · одна константа `BDO_ENV=PROD\|DEV` у `.env` для читання й запису разом; конфлікт із префіксом `BDO_API_ENV=` валить скрипт. З промпта диригента прибрано словник живої мови й виведення середовища. | [FLOW_STATE.md](../FLOW_STATE.md), розділ 2026-08-22 |
| `готово` | Повністю скриптовий flow видалено з репозиторію 2026-08-22; відновлення з коміта `dac631e`. Gate забороняє подавати його скрипти як команду в інструктивних файлах. | [FLOW_STATE.md](../FLOW_STATE.md), розділ 2026-08-22 |

## Відкладені рішення власника (повертатися свідомо)

| Статус | Задача | Де деталі |
|---|---|---|
| `чекає` | Поняття гри (`game_concepts` у серверному `/guide`) мусять дійти до CHILD-промптів, а не лише існувати в API. Перевірено 2026-08-28: `/guide` у наборі читає ЛИШЕ `cli/api/test-api.sh:34` для перевірки заголовка · ні воркер, ні QA, ні суддя його не бачать. Клас той самий, що й D9: дані є, поверхні немає. Класти в payload не можна · він осідає в транскрипті диригента назавжди (D10); правильне місце · build-time генерація секції в `.opencode/agent-templates/translation-{worker,qa,repair}.md` під час `./bdo env` із gate-звіркою (§8.9 це дозволяє). | [DEFECTS.md](DEFECTS.md) D9, D10 |
| `чекає` | Наповнити глосарій описами: поля `definition` і `wiki_url` існують у API з 2026-08-28 і вже доходять до воркера та QA, але порожні для ВСІХ термінів, тому вікі сьогодні не додає якості жодному рядку. Заповнювати через адмінку або `POST /glossary/proposals`; ефект видно з рядка «термінів N (з описом M)» у звіті payload. | [active/2026-08-27_FLOW_AND_SUBAGENT_ECONOMY.md](active/2026-08-27_FLOW_AND_SUBAGENT_ECONOMY.md), шоста зміна |
| `перевірити` | Підтвердити економію флоу на 2-3 пачках нового шляху: проходів моделі не більше двох повних, частка `REVIEW -> ai_layer` лишилась біля 30% (падіння = зростання ручної роботи), кількість дитячих викликів на пачку впала з пʼяти до трьох. Числа брати з `children` у manifest і `state/judge-decisions.jsonl`. | той самий план, Definition of Done |
| `чекає` | Калібрування судді рішеннями людини: звести вирок і впевненість із `state/judge-decisions.jsonl` з тим, що модератор зробив із рядком, і виводити «діапазон упевненості -> частка збігів». Без цього поріг `BDO_JUDGE_MIN_CONFIDENCE` лишається припущенням. Блокер: черга модерації порожня, ground truth ще не існує. | [backlog/2026-08-24_JUDGE_CALIBRATION_AND_MODEL_ANALYTICS.md](backlog/2026-08-24_JUDGE_CALIBRATION_AND_MODEL_ANALYTICS.md), J1 |
| `чекає` | Аналітика дефектів за моделями: звести модель child-сесії з бази OpenCode з русизмами, гомогліфами, зламаними токенами, не-PASS та інцидентами формату. Каталог Zen змінюється, а вибір моделі зараз тримається на разових вимірах. | той самий план, J2 |
| `перевірити` | `source_equivalent` закрито з обох боків 2026-08-24: сервер приймає `items[].same_as_source` і створює звичайну ревізію, набір ставить прапорець лише за вироком судді (не механічно за збігом). Лишилось звірити на живій пачці після деплою сервера. | той самий план, J3 |

## Як користуватися

1. Нову велику тему додати в `backlog/` і зареєструвати в `README.md`.
2. Почату тему перевести в `active/`.
3. Не видаляти залишок мовчки: позначити `відкладено` з причиною або передати в
   інший план.
4. Закрити задачу лише разом із доказом перевірки.
