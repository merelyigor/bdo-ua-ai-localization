# Черга робіт

Єдине місце для всього залишку роботи: великі теми посилаються на окремі плани,
дрібні задачі можуть жити без окремого файла.

- **Статуси:** `готово` · `в роботі` · `чекає` · `відкладено` · `перевірити`.
- Рядок має бути самодостатнім для нової сесії: ціль, шлях до плану й блокер.
- Статус оновлюється разом із кодом або документацією, які його доводять.

## Правила, безпека і документація

| Статус | Задача | Де деталі |
|---|---|---|

## Якість перекладу

| Статус | Задача | Де деталі |
|---|---|---|
| `готово` | Нерозпізнані назви (`probable_unresolved`) тепер у payload воркера і QA. Заодно виправлено хибне твердження, що такі рядки йдуть у модерацію: за замовчуванням вони ЗАПИСУЮТЬСЯ в ШІ-шар (`cli/batch/batch-commit.sh:136` вимагає `--names-to-moderation`). | [backlog/2026-08-22_TRANSLATION_QUALITY_IMPROVEMENTS.md](backlog/2026-08-22_TRANSLATION_QUALITY_IMPROVEMENTS.md), Q1 |
| `перевірити` | Суперечність про квадратні теґи прибрано в пʼяти місцях: правда тепер у `keep`, який справді перевіряє `Row::tokenViolations()`. Лишилось: додати `Title` -> `Титул` у глосарій (адмінка, `manage_glossary`) і звірити на живій пачці. | той самий план, Q2 |
| `готово` | `examples` тепер доходять і до QA: контекст зберігається в `context.json` теки пачки замість тимчасового файла, нуль додаткових викликів API. | той самий план, Q4 |
| `готово` | `examples` увімкнені за замовчуванням із трьома запобіжниками: проба перших 3 рядків, повторне використання `context.json`, деградація без прикладів при недоступному API. `--no-context` вимикає. | той самий план, Q3 |
| `готово` | Розмір пачки: дефолт 15 (рішення власника 2026-08-22), 10 для якості й 20 для швидкості. Змінено в промпті, `bdo help flow`, README і `docs/API.md`. Лишилось звірити частку не-PASS при N=15 на живому прогоні. | той самий план, Q5 |
| `готово` | Діагностика бенчмарка моделей (`./bdo bench`, команду знято разом із шаром OpenCode 2026-09-04): замість одного рядка «не JSON-масив» тепер видно content/reasoning/finish_reason/токени, уривок відповіді й підказку за симптомом; сира відповідь зберігається у `output/benchmark/`. | той самий план, S1 сусідній |
| `перевірити` | `translation-terminology` більше не читає пачку: вхід · `./bdo payload terminology` з готовим resolve, `read` прибрано з інструментів. Лишилось зміряти вхідні токени на живому прогоні · без чинного ключа `resolve` дає `no_answer`. | той самий план, S1 |
| `відкладено` | 86% рядків прод-прогону в ручному шарі дослівно збіглись із ШІ-перекладом. Три варіанти від 2026-08-17, вибір за власником. | той самий план, останній розділ |

## Архітектура й відказостійкість

| Статус | Задача | Де деталі |
|---|---|---|
| `в роботі` | **Зміна вектора 2026-09-04: основний інтерфейс · браузер, не TUI.** Локальні моделі лишаються, Docker не використовується, транспорт готується до зовнішніх API. Сесія роботи отримує історію по кожній пачці, а закриття сесії чистить живі файли. Порядок жорсткий: спершу макети в `docs/prototypes/`, потім план по етапах, і лише потім код. Перший макет готовий · живий прогін як чат із конвеєром. | [active/2026-09-04_BROWSER_GUI_AND_SESSIONS.md](active/2026-09-04_BROWSER_GUI_AND_SESSIONS.md) |
| `готово` | **Корінь тиску глосарія знято на сервері 2026-09-04.** Було: 131 391 запис із 136 022 має `ukrainian_layer: machine` при `severity: mandatory` (заміряно в нашому дампі `state/glossary-full.json`), тобто 96,6% «затверджених відповідників» є машинною здогадкою. Серверний агент підтвердив те саме число й назвав місце: `'ukrainian' => firstFilled(target_phrase, machine_target_phrase)`. Обидва пункти задеплоєно в правильному порядку; перша ж пачка після деплою дала карантин 0 замість 32%. | [active/2026-09-04_QA_SCOPE_AND_ROLES.md](active/2026-09-04_QA_SCOPE_AND_ROLES.md), D60, D61 |
| `готово` | Карантин був ≠ 0 за побудовою: відмова `glossary_violation` приходить у каналі МОДЕРАЦІЇ (`mode=proposal, auto_approve=false`), тобто рядку закрито обидва виходи, і сервер віддає його в кожну наступну пачку. 597 записів карантину на 84 унікальні рядки, один пройшов повний конвеєр 21 раз. Наш бік: журнал спроб `state/row-attempts.jsonl` зі стелею `BDO_ROW_MAX_ATTEMPTS` і прохід по назвах перед записом. Серверний бік задеплоєно 2026-09-04, і прогін `20260904_143842` дав 46 у шар, 4 у модерацію, 0 карантину. | [active/2026-09-04_QA_SCOPE_AND_ROLES.md](active/2026-09-04_QA_SCOPE_AND_ROLES.md), D56 |
| `готово` | Обсяг QA (заміряно на двох живих пачках 2026-09-04): QA бачить лише текст моделі, памʼять отримує PASS від коду після механіки (D57); модель більше не бачить `identity_hash` · короткі ключі `r1…rN` (D59). Частка часу QA впала з 60% до 18-19%, вихід на рядок з 126 до 44-85 токенів. | [active/2026-09-04_QA_SCOPE_AND_ROLES.md](active/2026-09-04_QA_SCOPE_AND_ROLES.md), D57, D59 |
| `чекає` | Сервер відхилив назву, вжиту у відмінковій формі («Торговця з ліхтарем»), хоча наш журнал казав, що сканер відмінки допускає. Потрібна відповідь серверного агента, за яких умов згадка зараховується й коли клієнт має право на `glossary_confirmed`. | [backlog/2026-09-04_GLOSSARY_INFLECTION_QUESTION.md](backlog/2026-09-04_GLOSSARY_INFLECTION_QUESTION.md) |
| `готово` | Сервер відхиляв назву, присутню в тексті дослівно · виправлено на сервері 2026-09-04 (коміт c120d9b, 4.7.8). Перевірено нашим прогоном: карантин упав з 30-50% до 9%, хибних відмов немає. | [DEFECTS.md](DEFECTS.md) D54 |
| `чекає` | Право слати `glossary_confirmed=true`, коли назва вжита у формі зі зміненою основою («Торговця з ліхтарем» для «Торговець з ліхтарем»): сервер підтвердив, що саме для цього випадку прапорець і призначений, прикриття синоніма чи старої назви ним заборонене. 3 рядки з 100 на замірі. | [DEFECTS.md](DEFECTS.md) D53 |
| `чекає` | Термін глосарія містить зайву лапку в назві (`Набір "Пам'ять майстра" II "`), через що правильний текст не зараховується. Глосарій ми не чіпаємо · виправляє людина через адмінку. | [DEFECTS.md](DEFECTS.md) D56 |
| `чекає` | 10% рядків пачки локальна модель перекладає без затвердженої назви · це вже справжня якість моделі, а не шум від сервера. Перше, що варто спробувати · окремий короткий прохід саме по назвах. | [DEFECTS.md](DEFECTS.md) D53 |
| `чекає` | Кожне правило промпта ролі мусить мати механічну перевірку (grep у gate або окремий тест): для локальної моделі правило без перевірки є лише побажанням. Залишок від закритого плану посилення промптів (план видалено разом зі старим проєктом 2026-09-04). | [AI_AGENT_RULES_REFERENCE.md](../AI_AGENT_RULES_REFERENCE.md), §8.11-8.14 |
| `чекає` | Розбір напрямку проєкту за вимірами 2026-08-26: ШІ-шар закритий на 97,9% (лишилось 29 936 рядків), 18% рядків пачки гинуть у `state/quarantine.jsonl` з `api_source_equivalent` і цим же спричиняють `no_progress`, диригент витрачає ~10 700 вхідних токенів платної моделі на рядок проти ~110 у воркера, а `improve` не має сигналу якості. | [backlog/2026-08-26_PROJECT_DIRECTION.md](backlog/2026-08-26_PROJECT_DIRECTION.md) |
| `перевірити` | Кросплатформенність: WSL-міст утратив предмет разом зі зняттям OpenCode 2026-09-04 · набір повністю працює в одному терміналі WSL. Лишилось підтвердити прогін на живій Windows-машині. | [WINDOWS_WSL2.md](../WINDOWS_WSL2.md) |
| `відкладено` | Порт оркестрації в `php bdo.php` (єдина залежність PHP 8.3, native Windows без WSL). Заміряно 2026-08-25: 44 з 49 `cli/**/*.sh` уже делегують у PHP. Не починати, доки WSL у одному терміналі покриває потребу. | [WINDOWS_WSL2.md](../WINDOWS_WSL2.md) |

## Відкладені рішення власника (повертатися свідомо)

| Статус | Задача | Де деталі |
|---|---|---|
| `готово` | Поняття гри доходять до child. Сервер: `GET /glossary/concepts`, 83 записи, `meta.complete: true`, поля `term/ua/gist/definition/wiki_url`. Набір: `cli/api/glossary-concepts.sh` тягне перелік ОДИН раз на прогін у `state/game-concepts.json` (TTL `BDO_CONCEPTS_TTL_HOURS`), а `worker-payload`/`qa-payload` кладуть у спільний блок `concepts` лише ті поняття, чий термін реально є в `source_text` пачки · ціле слово, з урахуванням регістру для скорочень (`MAP` це Monster AP, `map` · карта). У payload іде тільки короткий `gist`: `definition` до 4000 символів лишається людині, бо обрізаний опис модель читає як повний. | [DEFECTS.md](DEFECTS.md) D9, D10, D11 |
| `чекає` | Наповнити глосарій описами. Рішення власника 2026-08-28: НОВИЙ термін не пропонувати взагалі (глосарій наповнений), опис до наявного · лише коли впевненість у релевантності саме для BDO вища за 50%, і без довгої черги модерації. Зроблено: набір під час прогону РАХУЄ терміни без опису в `state/term-notes-queue.json` (частота + живі рядки) і показує пріоритет через `./bdo terms`; жодного запису в API. ЗАКРІПЛЕНО правило безпеки даних (D18): пропозиція можлива лише коли API ПРЯМО сказав, що опис порожній; відсутність поля означає «невідомо» і термін пропускається вголос. Майбутній відправник мусить перечитати стан терміна з API безпосередньо перед надсиланням і НІКОЛИ не міняти `ukrainian`. Зроблено 2026-08-28: роль `translation-glossary` пише `gist` і `definition` за живими рядками, `./bdo terms describe` будує завдання, `./bdo terms submit` надсилає пропозиції з порогом впевненості 60 і не більше 3 за раз, перечитуючи стан терміна з API безпосередньо перед записом. Ключ уже має `glossary:propose`; ендпоїнт приймає `definition` (до 4000) і `wiki_url`. | [DEFECTS.md](DEFECTS.md) D11 |
| `перевірити` | Підтвердити економію флоу на 2-3 пачках нового шляху: проходів моделі не більше двох повних, частка `REVIEW -> ai_layer` лишилась біля 30% (падіння = зростання ручної роботи), кількість дитячих викликів на пачку впала з пʼяти до трьох. Числа брати з `children` у manifest, `state/model-calls.jsonl` і `state/judge-decisions.jsonl`. | [DEFECTS.md](DEFECTS.md) D12 |
| `чекає` | Калібрування судді рішеннями людини: звести вирок і впевненість із `state/judge-decisions.jsonl` з тим, що модератор зробив із рядком, і виводити «діапазон упевненості -> частка збігів». Без цього поріг `BDO_JUDGE_MIN_CONFIDENCE` лишається припущенням. Блокер: черга модерації порожня, ground truth ще не існує. | [backlog/2026-08-24_JUDGE_CALIBRATION_AND_MODEL_ANALYTICS.md](backlog/2026-08-24_JUDGE_CALIBRATION_AND_MODEL_ANALYTICS.md), J1 |
| `чекає` | Аналітика дефектів за моделями: звести модель із `state/model-calls.jsonl` (поле `model` у кожному виклику) з русизмами, гомогліфами, зламаними токенами, не-PASS і вердиктами відмов. Раніше джерелом була база OpenCode, якої немає з 2026-09-04. | той самий план, J2 |
| `перевірити` | `source_equivalent` закрито з обох боків 2026-08-24: сервер приймає `items[].same_as_source` і створює звичайну ревізію, набір ставить прапорець лише за вироком судді (не механічно за збігом). Лишилось звірити на живій пачці після деплою сервера. | той самий план, J3 |

## Як користуватися

1. Нову велику тему додати в `backlog/` і зареєструвати в `README.md`.
2. Почату тему перевести в `active/`.
3. Не видаляти залишок мовчки: позначити `відкладено` з причиною або передати в
   інший план.
4. Закрити задачу лише разом із доказом перевірки.
