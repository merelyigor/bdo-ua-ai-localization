# Реєстр дефектів

Плани відповідають на питання «що робимо», цей реєстр — на питання «що ще
потребує рішення». Закриті дефекти живуть у regression-тестах і git-історії;
активний реєстр не дублює їхні квитанції.

## Правила ведення

1. Рядок додається в момент виявлення, а не після виправлення.
2. **Регресія обовʼязкова.** Закритий дефект називає тест або gate, який не дасть
   йому повернутись. Якщо механічної перевірки немає — статус лишається `відкритий`.
3. Доказ — команда, `файл:рядок` або вивід. «Полагоджено» без доказу не приймається.
4. Клас важливіший за випадок: повторюваний клас дефекту має коротке правило в
   канонічному документі, а не копію історії в цьому реєстрі.

## Статуси

`відкритий` — дефект відтворюється або не має механічної перевірки.
`закритий` — виправлено і має регресію; закриті записи видаляються з активного
реєстру після перевірки.
`прийнятий` — власник свідомо живе з ризиком; причина обовʼязкова.

## Активні та прийняті питання

| # | Дата | Симптом | Корінь | Клас | Статус | Регресія / наступне рішення |
|---|---|---|---|---|---|---|
| D224 | 2026-09-21 | Тестовий прогін на 5 рядків не стартував, хоча превʼю активного патча показувало нові рядки | dry-run перевіряв лише 10 сторінок; після 250 уже бачених identity він не доходив до першої нової сторінки й помилково повертав `no_work` | ліміт обходу не враховує локально пропущені сторінки | закритий | regression на 250 `run-seen` identity пройшла; браузер створив нову пачку `20260921_035331_34e7fa` на 5 рядків і запустив її |
| D223 | 2026-09-21 | Новий тестовий запуск без нових рядків показував стару завершену пачку замість явного повідомлення, що роботу не відібрано | `mode start` повертав успішний `complete` без створення нової пачки, а `current-batch` і стара `run-goal.json` лишалися чинними для Snapshot | stale current після порожньої вибірки | закритий | регресія в `tests/cli-run-mode-parity.sh`: `no_work`, код 3, старі `current-batch` і `run-goal.json` прибираються |
| D222 | 2026-09-21 | Слайдер показував 5 рядків, але створена пачка мала 50; під час продовження прогону драйвер також відкривав наступну пачку розміром 50 | Вибірка довіряла `limit` API й не обмежувала фактичний масив, а `run-loop` для наступної пачки читав глобальний дефолт замість розміру, збереженого в цілі прогону | розмір пачки розходиться між UI, API-вибіркою та драйвером | закритий | `tests/cli-api-fetch.sh`, `tests/cli-run-mode-parity.sh`, `tests/batch-summary.sh`, `tests/driver-loop.sh`; `./bdo gate touched` і `./bdo api` пройшли |
| D10 | 2026-08-28 | Payload диригента займав 69% транскрипту; 6 кроків = 1 162 696 вхідних токенів | OpenCode зберігає підставлений payload у частині повідомлення назавжди | вартість, якої ніхто не міряв | прийнятий | Прибрати з відкритої сесії неможливо (доведено). Помʼякшено: `-42%` вага, 5→3 виклики, облік у `state/session-load.json` |
| D12 | 2026-08-28 | Ефект економії (проходи, 30% `REVIEW -> ai_layer`, 3 виклики) не підтверджений на живих пачках | Зміни зроблено, прогону на новому шляху ще не було | заявлено без доказу | відкритий | немає перевірки · зняти з `children` у manifest і `state/judge-decisions.jsonl` |
| D53 | 2026-09-04 | Чисто локальні пачки віддавали в карантин 30-50% рядків | Дві третини відмов були серверним дефектом сканера глосарія, а не якістю моделі. Після його виправлення 2 пачки дали карантин 5 і 4 із 50 (9%), у шар 37 і 38, у модерацію по 8. Класи 9 карантинних: 5 · назви справді немає, 3 · назва у відмінковій формі зі зміненою основою, 1 · брудний запис глосарія із зайвою лапкою в назві | причина, приписана не тому шару | відкритий | лишилось 9% карантину трьох різних класів · кожен потребує свого рішення, див. `BACKLOG.md` |
| D188 | 2026-09-18 | Наказ глосарія, який вимагає вставити назву, для якої в тексті немає місця, не виконує жодна форма відповіді ролі. Рядок `f44f8313d4f1` двічі отримав `ужий «Exchange» для «Exchange»` на тексті, де слова `Exchange` немає: у PROD-пачці `20260918_044220` роль повернула повний текст без підстановки й рядок пішов у модерацію з `glossary_violation`; у тестовій `20260918_222153` та сама роль повернула тотожну правку (`find` = `replace`), код її відхилив | Наказ походить із серверної вимоги «термін має лишитись англійською», але місця для вставки в українському реченні він не називає. Підстановка за адресою фізично не може вставити слово без якоря, а підстановка повним текстом вимагає від моделі вигадати, куди його вставити | рядок щоразу проходить зайвий виклик ролі й однаково йде до людини | відкритий | немає перевірки · спершу потрібне рішення: або наказ такого виду не створювати взагалі, або сервер має казати місце вставки. Питання до серверного боку разом із D53 |
| D191 | 2026-09-19 | Наказ глосарія «термін лишається англійською» тепер виконується, але ламає українську граматику, і рядок із цим іде в PROD. Пачка `20260919_001137`: ремонт замінив «Обміняйте Всяку всячину» на «Exchange Всяка всячина», і рядок `f44f8313d4f1` пішов маршрутом `pass` | Наказ вимагає вжити слово, але не називає ні місця, ні відмінка; роль ставить його дослівно замість дієслова. Механічних дефектів немає, а QA дивилась на стан до ремонту | у ШІ-шар потрапляє нежива фраза, і помітити її можна лише очима | відкритий | немає перевірки · потрібне рішення: або наказ такого виду веде рядок до людини без спроби ремонту, або сервер називає місце й форму вставки (те саме питання, що й D188/D53) |
| D194 | 2026-09-19 | Термінолог тричі підряд не дав відповіді на пачці `20260919_011019` (28 термінів, payload 6.7 КБ): `stream_incomplete` за 1057 с, `thinking_loop` за 431 с, `not_json` за 208 с при 9 262 вихідних токенах, з яких відповіді немає; усе пішло в роздуми | Журнал викликів показує різницю моделей: та сама роль на тому самому payload відпрацювала на `huihui_ai/Qwen3.6-abliterated:35b-Claude-4.7-q4_K`, а на `qwen3.6:35b-mlx` провалила 3 з 3. Схема ролі обмежує відповідь, але не роздуми. Що на нашому боці могло погіршити — температура 1.0 проти власних 0.6 — не доведено, гіпотеза | прогін стоїть на першому кроці, і без ручного порівняння журналу не можна сказати, чи це модель, чи набір | відкритий | Наш бік закрито: рядок зупинки називає вирок, модель, час і вихідні токени (`RunDriveCommand::lastCallVerdict`), регресія · `tests/driver-loop.sh`. Сам зрив моделі перевіркою не закривається: вибір моделі є рішенням власника. Залишок — відкрите питання про температуру службових ролей |
| D200 | 2026-09-20 | Тестова пачка `20260920_055926_6e2f38` (`write:false`, звіт «Запису не було») показувала крок «запис» зеленою галочкою — так само, як пачка, що справді записала рядки в PROD | Стрічка кроків знала лише «стан пройдено», а право пачки писати (`write` у маніфесті) в неї не потрапляло взагалі | наслідок дії невидно на екрані: тест виглядає як бойовий запис | закритий | `tests/web-steps.sh` розділ 7 · крок віддає `dry` і підпис «без запису» лише при `write === false`; `write:true` і пачки без поля лишаються зеленими |
| D201 | 2026-09-20 | Сторож `tests/web-inner-html-guard.sh` падав на чистому дереві з `TypeError: window.requestAnimationFrame is not a function` і не перевіряв узагалі нічого; через нього гейт `touched` був червоний для кожного, хто чіпав `web/**` | Sticky-контекст хедера почав ставити себе через кадр анімації, а підроблене вікно сценарію такої функції не має | перевірка, яка падає до першої власної умови, мовчки перестає бути перевіркою | закритий | `tests/web-inner-html-guard.sh` · у підробленому вікні `requestAnimationFrame` викликає callback синхронно, тому код хедера реально виконується |
| D202 | 2026-09-20 | Живі лічильники токенів переписували `innerHTML` на кожен тік стану · рівно той клас ривків друку, проти якого стоїть сторож. Знайдено одразу, щойно сторож почав працювати | Лічильник малювався рядком розмітки, а не текстом у вже створеному вузлі | браузер щоразу заново розбирав розмітку під час живого друку | закритий | `tests/web-inner-html-guard.sh` · другий однаковий знімок не має права переписувати `innerHTML` жодного вузла, зокрема `#liveRequestTokens` |
| D203 | 2026-09-20 | Після переходу на модель у рантаймі oMLX у хедері лишались рівно два значення · `temp 1*` і `ctx 128k*`, тобто самі наші перекриття. Власні значення моделі (`top_p`, `top_k`, `min_p`, `presence_penalty`) зникли з екрана | Збирач каталогу читав власні значення моделі лише з Ollama (`/api/show`); гілка oMLX не брала їх узагалі, хоча admin API віддає їх у полі `settings` | хедер обіцяє показувати, з чим модель працює, і мовчки показує половину | закритий | `tests/model-selection.sh` · підроблений oMLX віддає `settings`, каталог мусить дати п'ять значень під спільними іменами, `null` і службові прапорці в параметри не потрапляють |
| D204 | 2026-09-20 | `tests/cli-payload-parity.sh` вимагала наказу «ужий» на прикладі, де назва в тексті ВЖЕ СТОЯЛА, тому повний гейт був червоний при справному коді | Перевірка стежила за однією половиною правила й лишилась із вчорашнім очікуванням після того, як код навчився бачити вже вжиту назву | червоний повний гейт ховає в собі всі інші перевірки, а агент починає «лагодити» робочий код | закритий | `tests/cli-payload-parity.sh` · три приклади: назви немає → наказ є; назва дослівно → наказу немає й сказано «уже виконано»; назва у відмінковій формі → наказу немає |
| D205 | 2026-09-20 | Рядок, у якому затверджена назва СТОЇТЬ, але у відмінковій формі, сервер відхиляв (`glossary_violation`), і набір віддавав його людині. У наступній вибірці рядок повертався знову | API має для цього прапорець `glossary_confirmed: true` (описаний у `/taxonomy` і `/guide` v6), а набір не слав його взагалі: `WritePayload` знав лише `same_as_source` | ручна черга наповнюється рядками, у яких переклад правильний | закритий | `tests/glossary-confirmed.sh` · прапорець ставиться ЛИШЕ на скаргу сервера, лише на людську назву, лише коли присутність доведена кодом і хоч одна назва не дослівна; кожну з умов фальсифіковано окремо |
| D206 | 2026-09-20 | Перевірка дизайну забороняла будь-яку чотиризначну `max-width` поза `:root` і через це валила повний гейт на звичайному медіа-запиті `@media (max-width:1100px)` | Правило відрізняло точку зламу від стелі ширини за кількістю цифр, а не за місцем | повний гейт червоний через те, що нічого не ламає | закритий | `scripts/agent-check.sh` · медіа-запит пропускається, стеля ширини в самому правилі ловиться далі (фальсифіковано доданням `.fake-wrap{max-width:1180px}`) |
| D207 | 2026-09-20 | Перевірка, яку неможливо виконати (немає node, curl, tmux), друкувала «пропуск» і виходила нулем. Гейт казав «Agent gate passed», хоча JavaScript сторінки не виконувався жодного разу | Пропуск і успіх мали однаковий код виходу, тому механізм перевірки не міг їх розрізнити за побудовою | хибне зелене гірше за червоне: воно вчить довіряти тому, чого не було | закритий | `tests/gate-skip-visibility.sh` · код 77 = «не виконано», гейт рахує й називає такі перевірки, підсумок про них каже, `BDO_GATE_NO_SKIP=1` робить пропуск падінням і саме так гейт іде в CI; жоден тест набору не має права вийти нулем після слова «ПРОПУЩЕНО» |
| D208 | 2026-09-20 | `gate touched` включав усі коміти від `origin/main`, тому серія непушених задач перетворювала селективну перевірку на майже повний прогін; CI окремо запускав `gate full` | Локальна селекція змішувала поточну зміну з історією гілки, а невідомі шляхи покладався перевіряти повний suite | кожна мала правка чекала хвилини; попередні спроби прискорити `full` не усували причину й повторювалися | закритий | `tests/gate-touched-map.sh` · локально береться лише робочий diff, CI передає точну базу push/PR, невідомий шлях валить gate; команда `gate full` видалена |
| D209 | 2026-09-20 | Паралельна гілка гейта втрачала код виходу тесту: під `set -e` підоболонка обривалась до запису коду, і читач підставляв 1 | Код писався наступною командою після самого тесту, тобто вже після того, як `set -e` обірвав підоболонку | будь-який код виходу перетворювався на «провал»; з появою коду 77 пропуск ставав падінням | закритий | `tests/gate-skip-visibility.sh` · обидва шляхи запуску (`BDO_GATE_JOBS=1` і `4`) перевіряються окремо на пропуску, успіху й провалі |
| D210 | 2026-09-20 | Статичний аналіз типів бачив саму теку `lib`. Майже дві тисячі рядків робочого PHP у `cli/**` і entrypoint `bdo` · той самий код, який запускає прогін у власника · не перевіряв ніхто, крім `php -l` | `phpstan.neon` мав `paths: [lib]` від моменту появи, а `bdo` ще й не має розширення `.php`, тому не потрапив би туди й за маскою теки | лінтер бачить лише синтаксис: помилка в імені класу, методі чи кількості аргументів проїжджала б до живого прогону | закритий | `tests/phpstan-scope.sh` · жоден робочий PHP-файл не має права опинитися поза `paths`, рівень названий явно, неіснуючий шлях валить перевірку (фальсифіковано вилученням `cli` і доданням `lib/NoSuchDir`); перевірка висить на кожній зміні `.php`, а не лише на конфігу. Виміряно перед розширенням: на рівні 0 нових помилок нуль |
| D211 | 2026-09-20 | Два місця, де код спирався на те, чого йому ніхто не обіцяв. `WorkerPayloadCommand` читав середовище API зі змінної, оголошеної лише всередині `try`: зв'язок «не вийшло взяти середовище → контекст не будуємо» тримався самим порядком рядків. `Snapshot::assemble()` і `Snapshot::stream()` описували форму своєї відповіді неповно · опис обіцяв 3 поля з 13, а читач уже спирався на `role`, якого в описі не було | Обидва дефекти народились однаково: опис форми даних писався один раз і не оновлювався разом із самими даними. Побачив їх лише статичний аналіз, і лише після розширення обсягу (D210) | сьогодні жодне з них не падає · це пастка на наступну зміну: одна нова причина лишити шлях контексту непорожнім, і код звернеться до неоголошеної змінної вже на живому прогоні | закритий | `tests/phpstan-scope.sh` + рівень 3 у CI · саме цей рівень ловить звернення до неіснуючого поля й читання неоголошеної змінної. Решту дев'ять зауважень розібрано того ж дня: сім виправлено, два визнано ХИБНИМИ (`$http_response_header` після невдалого `file_get_contents` справді не існує · перевірено на мертвому порту) і позначено окремим поясненням у коді. Опустити рівень нижче третього перевірка не дає |
| D212 | 2026-09-20 | Прогін CI упав на `tests/gate-skip-visibility.sh` · тесті, який САМЕ й доводить, що пропуск не є провалом. Крок гейта в CI працює з `BDO_GATE_NO_SKIP=1`, і ця змінна проходила наскрізь у вкладений запуск гейта всередині тесту: підроблений пропуск ставав падінням | Заборона пропусків описувалась змінною середовища, а середовище успадковується. Тест керував усім, крім самої умови, яку перевіряв | перевірка, яка залежить від того, хто її запустив, доводить не те, що написано в її назві | закритий | `tests/gate-skip-visibility.sh` · вкладений гейт запускається з явним `BDO_GATE_NO_SKIP=0`, а розділ заборони вмикає її сам; відтворено локально командою `BDO_GATE_NO_SKIP=1 bash tests/gate-skip-visibility.sh` |
| D213 | 2026-09-20 | Заборона пропусків у CI вимагала б виправити те, що виправленню не підлягає: бандл macOS на ubuntu-runner не перевіриться ніяким встановленням інструмента, а пропуск там рахувався так само, як відсутній `node` | Код 77 змішував дві різні речі · «інструмента немає, і це борг» та «ця платформа тут відсутня». Єдиним виходом із такого CI було б зняти заборону цілком, тобто повернути хибне зелене | борг і факт платформи в одному лічильнику роблять лічильник марним | закритий | `tests/gate-skip-visibility.sh` розділи 3b і 5b · код 78 = «не застосовно тут», рахується й називається окремо, `BDO_GATE_NO_SKIP=1` його НЕ валить (фальсифіковано підміною коду розпізнавання і вимкненням рядка підсумку) |
| D214 | 2026-09-20 | Чотири точки входу читали аргументи командного рядка зі змінної `$argv`, яка існує лише при увімкненому `register_argc_argv`; два описи параметрів у звіті запису були копіями сусідніх методів і називали параметр, якого в методі немає; замикання у звіті кроку захоплювало ширину екрана, якої не читало | Усе це той самий клас, що й D211: код і опис коду розійшлися, і жодна перевірка цього не бачила, бо статичний аналіз стояв на нульовому рівні | поодинці нешкідливо, разом · сім місць, де написане не відповідає дійсному | закритий | рівень 3 статичного аналізу в CI, межу стереже `tests/phpstan-scope.sh` (фальсифіковано опусканням рівня до 1). Аргументи тепер беруться з `$_SERVER`, який від налаштування рантайму не залежить |
| D215 | 2026-09-20 | Модель зациклилась у САМІЙ ВІДПОВІДІ, а не в роздумах: виклик `translation-worker` на пачці `20260920_234345_c983dd` намолотив 36 302 вихідні токени, повторюючи той самий абзац, і крутився б до стелі часу в 1500 с. Власник зупинив прогін руками | Детектор повторів спостерігав лише канал роздумів (`$isThinking`), а відповідь накопичувалась без жодного аналізу. Схема тут не рятує · constrained decoding є не в кожному рантаймі | безрезультатна робота на 20 хвилин, і єдиним рубежем лишався час, а не зміст | закритий | `tests/model-client.sh` · сценарій `answer_loop` падає з причиною `answer_loop`; саботаж порога доводить, що причина береться з детектора. Поріг ВИМІРЯНО, а не вгадано: 45 справжніх відповідей дали найгірші 44.7% повторів (45 однакових описів предмета · законний повтор), зациклена · 97.1%, межа 80%. На живому журналі детектор спрацював би на 5 146-му чанку з 36 637 |
| D216 | 2026-09-20 | Після натискання «зупинити» сторінка ще дві хвилини писала «прогін іде» й показувала картку «попередня роль завершилася · готується наступна роль · очікується запуск». Прогону вже не було: процес убито, наступної ролі ніхто не готував | «Прогін іде» трималось на свіжості журналу, а журнал лишається свіжим ще 120 с після смерті процесу. Підпис ручної зупинки весь цей час лежав у маніфесті пачки · його просто ніхто не питав | сторінка обіцяє роботу, якої ніхто не робить · власник не може відрізнити зупинку від зависання | закритий | `tests/web-steps.sh` розділ 8 · свіжий журнал без підпису читається як прогін, підпис зупинки гасить його одразу, а робота після зупинки робить підпис історією (фальсифіковано вимкненням умови). Рядок стану тепер каже «зупинено власником», а не «прогін не йде» |
| D217 | 2026-09-21 | JavaScript сторінки не перевіряв НІХТО · ~2 250 рядків у `web/app.js` і вбудованих скриптах шести сторінок. Синтаксичну поломку опосередковано ловили три node-тести, які цей код виконують; звернення до неоголошеної змінної, друкарська помилка в імені поля чи мертвий код проїжджали мовчки | У наборі не було ні лінтера JavaScript, ні самого Node як оснастки, хоча основний інтерфейс власника · саме браузерна сторінка | найбільша поверхня без механічної перевірки, і вона ж та, яку власник бачить щодня | закритий | `tests/web-lint.sh` · ESLint 10.11.0 прибитий версією, конфіг підібраний під наявний стиль коду (класичний скрипт, `var`, `== null`), перевіряються і файли, і ВБУДОВАНІ скрипти всіх сторінок через той самий конфіг. Тест сам доводить, що лінтер уміє червоніти (пробний файл зі зверненням до неоголошеного імені). У CI `npm ci` стоїть у тому ж завданні, тому пропуск там неможливий. Перша ж знахідка · мертва функція `unescapeJsonString` на 20 рядків, яку ніхто не викликав; прибрано |
| D218 | 2026-09-21 | Сторінка визначала живий процес власним `kill -0`, тоді як команда сервера для того самого питання вже вміла Windows через `tasklist.exe`. На рідному Windows команди `kill` немає взагалі: живий виклик моделі виглядав би завершеним, а картка ролі зникала б з екрана посеред роботи | Одне питання мало дві реалізації в різних місцях · класична розбіжність, яку ніхто не помічає, поки не змінить платформу (аудит A-06) | набір заявляє підтримку Windows, а сторінка там показувала б неправду | закритий | `tests/process-state.sh` · спільний вимірювач `lib/System/ProcessState.php`, платформа передається аргументом, тому гілку Windows проходить будь-яка машина; свій процес живий, завершений мертвий, власний `kill -0` у робочому коді заборонений перевіркою (фальсифіковано вимкненням гілки платформи й межі PID) |
| D219 | 2026-09-21 | CI перевіряв лише PHP 8.5, хоча README вимагає 8.3+. Синтаксис або функція з 8.4 проїхали б зеленими й зламались би в того, хто поставив заявлений мінімум | Мінімальна версія була написана в документації, але ніде не перевірялась (аудит A-08) | заявлена сумісність без перевірки є обіцянкою, яку ніхто не тримає | закритий | окреме завдання CI `PHP 8.3 (мінімальна заявлена)` · синтаксис усього робочого PHP плюс запуск набору (`help`, `env`, два unit-набори) без мережі, Ollama і GUI |
| D220 | 2026-09-21 | Правило «перед пропозицією відповідника API мусить ПРЯМО підтвердити, що поле порожнє» виконувалось лише для ОПИСУ терміна (`has_definition`, D18). Для самого ВІДПОВІДНИКА `Term::ukrainian()` віддавав `null` і коли сервер сказав «порожньо», і коли не сказав нічого, а `Row::pendingTerms()` трактував обидва однаково · як «відповідник треба придумати» | Звуження проєкції відповіді (інший endpoint, інша версія, бекенд із парою `term`/`translation`) зробило б КОЖЕН термін незатвердженим, і набір почав би вигадувати назви поверх затверджених людиною. Дорожче за опис: pending-термін іде не в чергу модерації, а просто в payload воркера. Знайдено інвентаризацією A-07 · правило трималося лише реченням у карті правил | найдорожчий клас: зіпсувати дані, які вже написала людина | закритий | `tests/glossary-unknown-state.sh` · три стани поля (є, порожнє, відсутнє), вирок «прогалин немає» заборонений при невідомому стані, обидва запобіжники фальсифіковані зняттям |
| D221 | 2026-09-21 | Селективний гейт не запускав тестів сторінки, які не звуться `web-*`: `tests/run-pause.sh` і `tests/model-selection.sh` читають `web/index.html`, але в карті `web/*` їх не було. Через це зміна рядка стану (D216) зламала `run-pause.sh` ще 2026-09-20, і провал лежав непоміченим · його показала лише повна категорія `gate shell` | Карта перевірок fail-closed для НЕВІДОМИХ шляхів, але fail-open для відомого шляху з неповним переліком тестів. Друга половина дефекту · сама перевірка чіплялась за дослівний рядок, тому багаторядковий тернарний вираз завалив її при незмінній поведінці | зелений селективний гейт перестає означати «нічого не зламано» | закритий | обидва тести додані в карту `web/*`; `tests/run-pause.sh` шукає по змісту, а не по рядку (фальсифіковано підміною `meta.push(s.paused` · перевірка червоніє) |
