# Реєстр дефектів

Плани відповідають на питання «що робимо», цей реєстр · на питання «що вже
ламалося і чим це закрито». Без нього дефект живе лише в чаті: за один день
2026-08-28 те саме коло «правило є, перевірки немає» повторилось чотири рази.

## Правила ведення

1. Рядок додається В МОМЕНТ виявлення, а не після виправлення.
2. **Регресія обовʼязкова.** Кожен закритий дефект називає тест або gate, який
   не дасть йому повернутись. Якщо механічної перевірки немає · так і пишеться
   `немає перевірки` і рядок лишається `відкритий`, скільки б код не правили.
3. Доказ · команда, `файл:рядок` або вивід. Формулювання «полагоджено» без
   доказу не приймається.
4. Клас важливіший за випадок: якщо дефект належить до класу («правило без
   перевірки», «тихий збій»), це називається прямо · саме клас підказує, де
   шукати наступний.

## Статуси

`відкритий` · дефект відтворюється або не має механічної перевірки.
`закритий` · виправлено І має регресію.
`прийнятий` · рішення власника жити з ним; причина обовʼязкова.

## Реєстр

| # | Дата | Симптом | Корінь | Клас | Статус | Регресія |
|---|---|---|---|---|---|---|
| D1 | 2026-08-27 | Диригент переписував payload у аргумент `task`; на пачці 50 рядків виклик розвалювався на розборі JSON (позиція 76134) | Правило «передавай лише посилання» жило тільки в промпті | правило без перевірки | закритий | `tests/driver-loop.sh` · `referencesStagedPayload()` |
| D2 | 2026-08-28 | Після відмови диригент чув «Tool invalid is unavailable» замість причини і повторював те саме | Зламані аргументи OpenCode подає інструментом `invalid`, guard відповідав загальним текстом | незрозуміла відмова | закритий | `tests/driver-loop.sh` · `unparsableTaskHint()` |
| D3 | 2026-08-28 | Пачка стала намертво в `healing`; `run drive` падав із «Заборонений перехід стану» | Код почав робити `healing -> awaiting_judge`, машина станів його не дозволяла | зміна коду без зміни машини станів | закритий | `tests/mechanical-before-qa.sh` · `StateMachine::assertTransition` |
| D4 | 2026-08-28 | Я двічі заявив, що `translation-repair` не викликається, хоча він брав 37 рядків із 50 | `journal.jsonl` писав лише стани; repair живе всередині `healing` | стан, який ніхто не читає | закритий | `tests/drive-memory-layers.sh` · `child_dispatch:*` |
| D5 | 2026-08-28 | Я сказав «сесія мовчить», хоча вона двічі отримала відмову guard | Дивився на стан пачки й `tokens_input`; відмовлений виклик не рухає ні те, ні інше | дебаг не тим інструментом | закритий | `./bdo session`, перевірка в `tests/mechanical-before-qa.sh` |
| D6 | 2026-08-28 | Власник мусив просити «напиши текстом», щоб дізнатися причину зупинки | Промпт не зобовʼязував диригента звітувати самому | мовчазна зупинка | закритий | `scripts/agent-check.sh` · `ЗВІТ ПРО ПРОБЛЕМУ пиши САМ` |
| D7 | 2026-08-28 | Диригент двічі намагався сам пропатчити `StateMachine.php` через `php -r` | Інструменти запису вимкнені, але промпт не казав, що робити замість | межа без альтернативи | закритий | `scripts/agent-check.sh` · `Код набору НЕ лагодь` |
| D8 | 2026-08-27 | Локальна збірка вставляла китайські ієрогліфи в український текст, жоден детектор не бачив | `Homoglyphs` шукає латиницю, `Russianisms` · російські слова; чужої писемності не перевіряв ніхто | клас дефекту без детектора | закритий | `tests/mechanical-final-check.sh` · `ForeignScript` |
| D9 | 2026-08-27 | На «що є на покращення?» диригент читав `docs/plans/BACKLOG.md` замість рядків | Обсяг режиму `improve` не показувала жодна команда | дані без поверхні | закритий | `tests/domain-filter.sh` · розділ «Доступно на покращення ШІ» |
| D10 | 2026-08-28 | Payload диригента займав 69% транскрипту; 6 кроків = 1 162 696 вхідних токенів | OpenCode зберігає підставлений payload у частині повідомлення назавжди | вартість, якої ніхто не міряв | прийнятий | Прибрати з відкритої сесії неможливо (доведено). Пом'якшено: `-42%` вага, 5→3 виклики, облік у `state/session-load.json` |
| D13 | 2026-08-28 | Child-моделі перекладали, не знаючи, що це Black Desert Online: у промптах гра згадувалась лише як токен «`Black Desert` не перекладай» | Рамки задачі не було взагалі, тому знання гри з навчання моделі не задіювалось, а порожнє місце заповнював спогад про російську локалізацію | контекст без рамки | закритий | `scripts/agent-check.sh` · перевірка `КОНТЕКСТ РОБОТИ` у пʼятьох child |
| D14 | 2026-08-28 | 3 з 11 рядків пачки пішли до людини лише через регістр: глосарій вимагає `Записи`, `Бамбук`, `Рік`, модель написала з малої | Різниця тільки у великій літері вважалась дефектом нарівні зі змістовими; виправляти її було нікому | однозначне виправлення віддано людині | закритий | `tests/mechanical-before-qa.sh` розділ 7 · `Row::fixGlossaryCase()` |
| D15 | 2026-08-28 | Диригент видав `task` без `subagent_type` і без `prompt`; OpenCode створив звичайного агента, той завис у `pending`, пачка стала в `awaiting_worker` | Обидва плагіни пропускали такий виклик: child-contract виходить раніше, бо роль не `translation-*`, а execution guard перевіряв лише `translation-*` ролі | межа, що перевіряє тільки очікуваний випадок | закритий | `tests/driver-loop.sh` · блок «Task БЕЗ дозволеної ролі» |
| D16 | 2026-08-28 | Відмову guard неможливо було перевірити: у транскрипті лишалось акуратне `payload:<шлях>`, тому справжнє порушення виглядало як хибне спрацювання (я на цьому помилився в звіті) | Стискання аргументу перед кидком помилки рятувало контекст, але знищувало доказ | виправлення, що знищує свідчення | закритий | `tests/driver-loop.sh` · `state/prompt-violations.jsonl`, видно в `./bdo review` |
| D17 | 2026-08-28 | `translation-repair` не стартував тричі поспіль: guard відхиляв виклик, диригент повторював те саме, пачка стала намертво | Відмова замість виправлення. Модель копіювала ВЛАСНІ приклади з транскрипту, де успішні виклики мають `prompt` = повний payload (його підставляє плагін, і стиснути запис назад неможливо), а `read` віддавав обрізану копію · у журналі 3284 символи при файлі 4650 | покарання замість доставки | закритий | `tests/driver-loop.sh` (підстановка замість відмови) + `tests/driver-loop.sh` (заборона `read` payload) |
| D18 | 2026-08-28 | Черга описів термінів могла запропонувати опис поверх уже написаного людиною | Порожні поля викидаються з payload заради ваги, тому «немає ключа `definition`» читалось однаково і як «опису немає», і як «сервер про нього не сказав» | відсутність даних прийнята за відсутність значення | закритий | `tests/payload-shared-examples.sh` · прапорець `has_definition` і голосний пропуск невідомого стану |
| D19 | 2026-08-28 | `./bdo gate docs` заявив, що `docs/FLOW_STATE.md` посилається на відсутній `term-notes-submit.sh`, хоча файл є в `cli/api/` | Резолвер посилань знав фіксований список каталогів (`docs/`, `scripts/`, `roles/`) і не бачив дерева команд `cli/**` | список замість пошуку | закритий | `scripts/agent-check.sh` · пошук голого імені серед `git ls-files`; перевірено, що вигадане імʼя далі валить gate |
| D20 | 2026-08-28 | Жоден child не стартував: `Task` падав `OPENCODE_RUNTIME_INVALID: fingerprint does not match the effective model policy`, `./bdo env` не допомагав (сесія `ses_fb8c1689cffe0M4L3ECUwdVkfw`, дві спроби воркера) | Сьома роль `translation-glossary` додана в `ModelPolicy::ROLES`, а читач `cli/model/client.php` мав власний список із шести ролей: materializer рахував відбиток по семи маршрутах, плагін · по шести | два списки того самого | закритий | `tests/model-client.sh` · справжній PHP-materializer проти справжнього читача; `tests/driver-loop.sh` бере ролі з шаблону політики |
| D21 | 2026-08-28 | Після виправлення D20 перший же `./bdo run drive` віддав `child_no_response` і підказку «перевір модель субагентів у `.env`», хоча модель була справна: лічильник уже стояв на 3 | Лічильник мовчання не відрізняв відмову провайдера від відмови НАШОГО плагіна: в обох випадках немає ні файла відповіді, ні інциденту формату | своя помилка порахована як чужа | закритий | механізм прибрано 2026-09-04 разом із плагінами: лічильника мовчання більше немає, бо мовчазного child не буває · `cli/model/client.php` падає з машиночитаною причиною й журналює КОЖЕН виклик (`tests/model-client.sh` перевіряє всі шляхи відмови), а видно їх через `./bdo incidents` за `state/model-calls.jsonl` |
| D22 | 2026-08-28 | Пачка `20260828_100456`: 24 з 50 рядків у модерацію, з них 11 · один і той самий `Cheongsa Island` у трьох варіантах, хоча відповідник «Острів Ліхтарів» затверджений і повертається API | Пачковий запит `POST /rows/context` тихо не вдався, і код деградував до «робочого payload зі слабшим сигналом»: затверджені терміни `severity=mandatory` до моделі не доїхали (`terms.json` = `[]`, `глосарій 0`) | деградація там, де потрібна зупинка | закритий | `tests/payload-shared-examples.sh` · заглушка HTTP + перевірка, що крок падає, а `run drive` віддає `context_unavailable` |
| D23 | 2026-08-28 | `Sаmоtня альтанка серед природи.` пройшла механіку як чистий рядок; латинські `S`, `а`, `m`, `о` знайшла вже модель QA, і рядок пішов у модерацію як critical | `Homoglyphs::find()` повідомляв лише про слова, які ВМІВ виправити (`$fixed !== $word`): у `Sаmоtня` латинські `S` і `m` кириличних двійників не мають, тому слово мовчки випадало разом із виправними `а` та `о` | детектор мовчить про те, чого не вміє полагодити | закритий | `tests/mechanical-final-check.sh` · блок 2б (`Defects` мусить назвати «змішана абетка») |
| D24 | 2026-08-28 | Пачка `20260828_131740`: QA сперечається з глосарієм · «Острів Ліхтарів» проти прикладів «Острів Чхонса», «Торговець з ліхтарем» проти «Торговці Чоронга»; рядки йдуть у модерацію на рівному місці | У payload одночасно їдуть `terms` (затверджений відповідник) і `examples` (попередні переклади, зроблені ДО затвердження); модель порушує одне з двох, хоч що вибере | два джерела правди в одному payload | закритий | `tests/payload-shared-examples.sh` · `GlossaryExamples::filter` (перевірено: зі зламаним фільтром тест падає) |
| D25 | 2026-08-28 | Власник сказав «треба доперекласти всі knowledge», диригент закрив пʼяту пачку й запитав «Продовжувати?», хоча за фільтром лишався 141 рядок; ціль довелось повторювати вручну після кожної пачки | Ціль прогону жила лише в тексті чату, а `run drive` наприкінці пачки віддавав `complete` · це читається як «роботу зроблено». Стискання сесії лишило формулювання цілі, але не обовʼязок продовжувати | ціль без носія в стані | закритий | `tests/batch-summary.sh` · `continue_run`/`goal_complete` (перевірено: з поверненням `complete` тест падає) |
| D26 | 2026-08-28 | `./bdo batch check` під час розбору відповів `batch-assert.sh: line 15: 1: Потрібен rows.json` · сире bash-повідомлення з номером рядка замість відповіді на питання | Документована діагностична команда вимагала аргумент, якого реєстр не показує; `${1:?}` друкує посилання на рядок скрипта, а не причину | причина, схована за синтаксисом оболонки | закритий | `tests/no-silent-failures.sh` розділ 3 (перевірено: зі старою поведінкою падає) |
| D27 | 2026-08-28 | Запит до ще не розгорнутого `/glossary/terms/list` виглядав як зависання на дві хвилини замість помилки; те саме раніше з `GET /glossary/terms` без `q` | У HTTP-клієнті стояв `--retry-all-errors`, тому постійні 400/401/403/404 повторювались усі 570 секунд бюджету · це суперечило власному заголовку файла («408/429 і 5xx») | повтор там, де наступна спроба дасть те саме | закритий | `tests/http-retry.sh` · перевірка РЕАЛЬНИХ аргументів curl |
| D28 | 2026-08-29 | Child `translation-qa` тричі поспіль повернув порожню відповідь на локальній `qwen3.6:35b-a3b-mtp-q4_K_M`; помилки не було, пачка стала перед QA | У `.env` стояло `BDO_REASONING_EFFORT=off`, а це «не надсилати поле», тобто лишити поведінку провайдера · для думальної моделі це ДУМАТИ. Відповідь ішла в `reasoning`, `content` лишався порожній. Крок `./bdo runtime` показував OK, бо проба завжди слала зашите `none` замість значення з `.env` | «вимкнено» означало «не сказано»; перевірка йшла не тим шляхом, що робота | закритий | `cli/runtime/check-runtime.sh` крок 3 бере ефективне значення з `.env` і при `off` прямо називає причину. **ПЕРЕВИМІРЕНО 2026-09-04 на Ollama 0.33.3 · висновок про несумісність думання БІЛЬШЕ НЕ ЧИННИЙ:** `think=false` дає 7.6 с і 150 символів валідного JSON, `think=true` · 44.8 с і 96 символів ТЕЖ валідного JSON плюс 6 544 символи в окремому полі `thinking`. Тобто порожнього `content` немає; є ціна вшестеро. Через це думання стало вибором (`BDO_MODEL_THINK=1`, типово вимкнене), а не забороною; `tests/model-client.sh` п. 12д тримає межу «роздуми не є відповіддю: порожній `content` лишається відмовою» |
| D29 | 2026-08-28 | Пачка `quest` (61 рядок) тричі поверталась на крок QA; у базі OpenCode видно чому: спроба о 20:53 віддала РІВНО 16 384 вихідні токени, тобто відповідь обірвано на стелі й JSON став невалідним | `cli/runtime/check-runtime.sh` вписував у конфіг OpenCode `"output": 16384` для всіх локальних моделей незалежно від вікна (у qwen3.6 воно 262 144) | стеля, вигадана нами, а не моделлю | закритий | `tests/model-client.sh` · виявлення застарілої стелі, підняття через `./bdo models --apply`, ЦІЛІСНІСТЬ конфігу після правки й недоторканість чужих провайдерів |
| D30 | 2026-08-29 | Диригент після успішного QA (`Збережено відповідь translation-qa: 50 елементів. Далі: ./bdo run drive.`) написав звіт про стан і завершив хід; пачка `20260829_014214` лишилась в `awaiting_qa`, і продовжити мусив власник | Сигнал у виводі інструмента БУВ, але продовження лишалось рішенням моделі; жоден механізм не тримав хід у роботі | обовʼязок, який тримає лише промпт | закритий | `tests/driver-loop.sh` · плагін `translation-autopilot` штовхає ЛИШЕ незавершену пачку, лічильник на пару «пачка+стан», журнал `state/autopilot.jsonl` |
| D31 | 2026-08-29 | Категорія `market` є в `RunSpec::DOMAINS` і в живому API, але guard відхиляв `./bdo mode start … market` у ВСІХ чотирьох режимах; побачити це можна було лише спробувавши | Перелік категорій жив двічі: у коді набору й у regex `cli/command-registry.json` | два списки того самого (той самий клас, що D20) | закритий | `tests/domain-filter.sh` · кожна категорія з `RunSpec::DOMAINS` мусить проходити guard, вигадана · ні |
| D32 | 2026-08-29 | У конфізі OpenCode оголошено вікно 262 144, а Ollama піднімає модель зі 131 072 (повзунок «Context length» у застосунку): OpenCode не почав би стискати вчасно, а llama.cpp викидав би початок розмови з `n_keep = 4` мовчки. Сесія диригента на момент виявлення була вже на 88 870 токенах | Вікно бралося з `ollama show` (максимум МОДЕЛІ), а не з реального рантайму | оголошена межа більша за справжню | закритий | `tests/model-client.sh` · завищене вікно ловиться, `./bdo models --apply` вирівнює; без даних про рантайм скрипт нічого не вигадує |
| D33 | 2026-08-29 | Кожен коміт падав `api_write_failed`; пачка `20260829_025045` (50 готових рядків) стала в `committing`, власник чотири рази поспіль отримав ту саму помилку без причини | Я додав перевірку права на канал і ВИГАДАВ форму відповіді: читав `data.writes.channels` як мапу за назвою каналу, тоді як сервер віддає СПИСОК обʼєктів `{layer, mode, allowed, result}`. Перевірка не знаходила запису й блокувала все | контракт вигаданий, а не прочитаний | закритий | `tests/write-channel-rights.sh` · зразок узято з живої відповіді PROD; зі старою формою тест падає |
| D34 | 2026-08-29 | Власник просив перекласти ВЕСЬ патч 7; диригент закрив категорію `entity`, отримав `goal_complete` і став чекати рішення, хоча в патчі лишалось понад тисяча рядків | Ціль у `run-goal.json` була рівна КАТЕГОРІЇ, тому вичерпана категорія читалась як досягнута ціль | ціль вужча за намір власника | закритий | `tests/batch-summary.sh` · вичерпана категорія переводить ціль на ПАТЧ без категорії, порожній патч · `goal_complete` |
| D35 | 2026-08-29 | Диригент отримав голе `{"kind":"continue"}` і вголос припустив причину («мабуть, усі терміни вже мають відповідники»), тобто вгадав стан набору | Шість конвертів `continue` не мали поля `reason`; набір знав причину, але не казав її | здогад замість відповіді | закритий | `tests/no-silent-failures.sh` розділ 4 · у `run drive` не лишилось жодного `continue` без причини |
| D36 | 2026-08-29 | Диригент почав «відновлювати» вміст payload по памʼяті й сам написав власнику, що «продовжувати з вигаданими проміжними payload безвідповідально при живих записах у PROD»; 106 записів у `state/prompt-violations.jsonl` | Плагін підставляв staged payload МОВЧКИ: для моделі це виглядало так, ніби її саморобний payload і є тим, що отримав child. Дані були цілі, страждали токени й довіра | тихе виправлення читається як згода | закритий | `tests/driver-loop.sh` · після підстановки диригент бачить рядок «твій аргумент prompt проігноровано», а на правильному виклику попередження немає |
| D37 | 2026-08-29 | Текст ПІСЛЯ repair до сервера не показувався: валідація йшла лише один раз, до QA. Якщо саме виправлення порушувало глосарій, ми дізнавалися про це на записі, і рядок падав у карантин замість модерації | Валідація стояла в одному місці конвеєра, хоча текст після неї ще двічі змінюється (repair, суддя) | перевірка до останньої зміни даних | закритий | `tests/mechanical-final-check.sh` · відмова API на фінальній валідації знімає рядок із запису й веде до людини (перевірено: без урахування відмови тест падає) |
| D38 | 2026-08-29 | `OPENCODE_RESTART_REQUIRED` не називав, ЩО саме змінилось: диригент видав власнику пʼятнадцять абзаців припущень (фоновий `git pull`? сесія не перезапущена? зміна за секунди?), хоча причина була одна · правка `cli/run/run-drive.sh` між його `./bdo platform` і `./bdo mode status` | Відбиток рахувався одним хешем по 26 файлах, тому назвати винного було нічим | межа без пояснення штовхає на здогад | закритий | `tests/driver-loop.sh` · відмова мусить містити імʼя зміненого файла |
| D39 | 2026-08-29 | Диригент завис на кроці QA пачки `20260829_222317`: три «продовжуй» поспіль без жодного виклику, автопілот вичерпав поштовхи за 8 секунд. У міркуваннях: «я не маю точної копії 49-рядкового staged файла» | Моє ж повідомлення «УВАГА: твій аргумент проігноровано» модель прочитала як вимогу ДАТИ правильний вміст; плюс після стискання контексту вона втратила `next.prompt` і не знала, що його можна перезапитати | сигнал, що додає роботи замість того, щоб її зняти | закритий | `tests/driver-loop.sh` · текст мусить бути спокійним (без «УВАГА/проігноровано») і прескриптивним; у CHILD-КОНТРАКТ додано рядок «немає next.prompt · виконай run drive ще раз» |
| D40 | 2026-08-29 | Диригент тричі поспіль відповів «продовжую негайно» і жодного разу не викликав Task; сам це визнав («я щоразу пишу продовжую і не викликаю Task»). Автопілот вичерпав поштовхи й замовк у журнал · власник просто дивився, як нічого не відбувається | Сесія набрала 1,96 млн вхідних токенів; після стискання модель перестала доводити крок до дії. Здача автопілота була видима лише в `state/autopilot.jsonl` | зупинка, про яку ніхто не сказав | закритий | `tests/driver-loop.sh` розділ 7 · після вичерпання поштовхів набір показує сповіщення з порадою почати нову сесію, і не повторює його |
| D41 | 2026-08-29 | QA двічі поспіль повернула 48 вироків із 49, щоразу пропускаючи той самий хеш (`Checkmate: Queen Bundle`); пачка ходила колами, 48 готових вироків щоразу викидались, а власник тричі написав «продовжуй» без руху | `assertCoverage` вимагає вирок на кожен рядок (і правильно), але прогалину не було чим закрити · лишався тільки повтор або зупинка всієї пачки | правило без запасного виходу | закритий | `tests/pipeline-faults.php` · `qa-coverage-fill.sh` добиває прогалину чесним `REVIEW/minor` (перевірено: якщо добитий вирок піде в шар, тест падає) |
| D42 | 2026-09-03 | Маршрут провайдера з власним endpoint (`opencode-omniroute`) не перевірявся на оголошення: `./bdo models` друкував йому `ЗОВНІШНІЙ` і йшов далі. Профіль `session-omniroute` із пʼятьма `auto/*` маршрутами дав би чотири порожні дочірні сесії · нуль токенів, жодної помилки | Гілка «не ollama · значить каталог є в OpenCode» вірна для `opencode/*` і `openai/*`, але провайдер із `options.baseURL` каталогу не має взагалі | межа, звужена до одного провайдера, тоді як клас ширший | закритий | `tests/model-client.sh` · `cli/runtime/check-runtime.sh` кличуть і робота, і тест (перевірено трьома саботажами: сліпий довідник, довідник-панікер, скрипт без виклику) |
| D43 | 2026-09-03 | У `.env.example` рядок `TRANSLATE_MODEL=opencode/big-pickle, TRANSLATE_MODEL_COST=free.` не був коментарем: у речення «Повернутися на безплатний: …» загубився `#`. Копія шаблону в `.env` задавала моделлю рядок із комою й крапкою, і ключ `TRANSLATE_MODEL` був у файлі двічі з різними значеннями | Жоден gate не перевіряв `.env.example` як файл значень · лише як документацію | шаблон, який ніхто не читає машиною | закритий | `tests/model-client.sh` пункти 1-2 · дубльований ключ і `TRANSLATE_MODEL` не у формі `provider/model` (перевірено: із поверненим рядком тест падає) |
| D44 | 2026-09-04 | `./bdo audit` друкував `SHAPE не JSON-масив (проза або обірвано)` для `translation-qa` і `translation-repair` на кожній ПРАВИЛЬНІЙ відповіді, поки пачки `20260904_003903` і `20260904_004221` спокійно клали 49 рядків із 50 у шар | Аудит вимагав голий JSON-масив, тоді як схема запиту вимагає конверт `{"items":[…]}` (`cli/prepare/build-schema.sh`), і плагін саме так його й розпаковує (`cli/model/client.php`). Перевірка розійшлася з власним контрактом | хибна тривога, що привчає не читати звіт | закритий | `tests/audit-response-shape.sh` · `cli/audit/response-shape.php` кличуть і аудит, і тест (перевірено двома саботажами: повернення вимоги голого масиву й сліпота до повтореного хеша) |
| D45 | 2026-09-04 | Сесія диригента вмерла на 11-й пачці й більше не приймала «продовжуй»: `Input exceeds context window for opencode-zen/big-pickle: estimated 211204 input tokens, limit 200000`. Кожне наступне «продовжуй» давало ту саму помилку | Маршрут диригента `opencode-omniroute/auto/best-coding` був оголошений у конфізі OpenCode БЕЗ `limit` узагалі, тому OpenCode не знав межі й не стискав розмову · сесія набрала 2 420 005 вхідних токенів. `./bdo models` перевіряв лише НАЯВНІСТЬ ключа моделі й друкував `OK` | той самий клас, що D32, у крайній формі: межі не завищено, а не оголошено зовсім | закритий | `tests/model-client.sh` · причина `no-limit` у `cli/runtime/check-runtime.sh`, `./bdo models --apply` доливає `limit` у наявний запис (перевірено саботажем: без вимоги `limit.context` тест падає) |
| D46 | 2026-09-04 | `cli/run/run-loop.sh` падав `line 123: mode\xc2: unbound variable` рівно на тому рядку, який мав НАЗВАТИ причину зупинки · тобто зупинка ставала мовчазною саме тоді, коли пояснення потрібне найбільше | `«$mode»`: bash затягує перший байт багатобайтового `»` в імʼя змінної, тому `$mode` перетворюється на неіснуючу `mode\xc2`. Українські лапки в повідомленнях проєкту всюди | конструкція, що ламається лише на не-ASCII сусідові | закритий | `tests/driver-loop.sh` пункти 3-4 (зупинка мусить містити причину) · перевірено на всіх `.sh` репозиторію: змінна перед не-ASCII символом пишеться у фігурних дужках |
| D47 | 2026-09-04 | `cli/model/unwrap.php` розгортав `{"items":{"a":1}}` у список значень обʼєкта, тобто ТИХО підміняв відповідь моделі іншими даними | У PHP `is_array` істинний і для асоціативного масиву, тому перевірки «це конверт зі списком» не було; бракувало `array_is_list` на самому `items` | перевірка типу, що не розрізняє список і обʼєкт | закритий | `tests/schema-provider-compat.sh` · випадок `{"items":{"a":1}}` мусить лишитись незмінним |
| D48 | 2026-09-04 | `./bdo timer` показав «минуло 3 год», коли реально минуло 6: таймер, заведений щоб не перевищити межу, сам про неї брехав | Час зберігався локальним рядком і читався через PHP `strtotime` з іншою типовою таймзоною (зсув 3 години) | одиниця виміру без зафіксованої точки відліку | закритий | `cli/system/session-timer.sh` зберігає epoch-секунди, а форматує їх `date -r`; таймзони в розрахунку немає взагалі |
| D49 | 2026-09-04 | `./bdo models` друкував `ЗОВНІШНІЙ` для маршруту, а `./bdo runtime` перевіряв оголошення моделі в чужому конфізі · обидві перевірки після зняття OpenCode перевіряли б неіснуючий шар | Перевірка була привʼязана до застосунку, а не до нашого контракту | перевірка живе в чужому файлі | закритий | `cli/runtime/check-runtime.sh` крок 5 звіряє `config/roles.json` з `ollama list`, а кроки 3-4 роблять справжній виклик `cli/model/client.php` · той самий файл, що працює на пачці |
| D50 | 2026-09-04 | Меню передало `./bdo mode start патч 50 7`, і прогін упав `RunSpec::preset('патч')` вже ПІСЛЯ вибору патча й підтвердження; наступна пачка драйвера зупинилась «невідомий режим цілі «patch»» · два боки того самого розриву | Ключі режимів у `lib/Pipeline/RunSpec.php` англійські (`patch`, `manual`, `proposal`, `improve`), а власник і документація знають їх українською. Раніше цей переклад робила в голові модель-диригент, і при її знятті перекладати стало нікому | назва, яку переводив тільки здогад | закритий | `tests/tui.sh` пункт 6 (меню шле КЛЮЧ, не підпис) і `tests/driver-loop.sh` пункт 3б (українська назва в цілі зупиняє драйвер) · переклад робить `mode_key()` у `bin/tui.sh`, одне місце |
| D51 | 2026-09-04 | Перша ж пачка після зняття OpenCode стала на першому кроці: `missing_schema: немає state/current-response-schema.json` для `translation-terminology` | Схеми ролей `terminology`, `judge` і `smoke` не залежать від рядків пачки й лежали КОНСТАНТАМИ в TS-плагіні `translation-routing-guard`. Тобто формат відповіді ролі був описаний у двох місцях (промпт словами, плагін схемою) і зводив їх чужий застосунок; разом із плагіном зникли й схеми | опис контракту, що живе поза проєктом | закритий | `roles/schema/*.json` поруч із промптами, джерело схеми назване в `config/roles.json` (`response`, `qa`, `file:<шлях>`); `scripts/agent-check.sh` і `tests/pipeline-unit.php` падають, якщо роль посилається на відсутній файл схеми |
| D52 | 2026-09-04 | Друга пачка прогону зупинилась повідомленням `ЗУПИНКА: невідомий крок «-» у стані -` · тобто драйвер назвав НАСЛІДОК замість причини й виглядав зламаним на рівному місці | `./bdo run drive` законно друкує людські звіти перед конвертом (рядок `payload QA: 50 рядків, глосарій 26`), а драйвер декодував увесь stdout одним `json_decode` | розбір, що вважає весь вивід одним значенням | закритий | `tests/driver-loop.sh` пункти 4б і 4в: звіт перед конвертом не ламає прогін, а повна відсутність конверта дає причину «немає конверта» з останніми рядками виводу |
| D53 | 2026-09-04 | Чисто локальні пачки віддавали в карантин 30-50% рядків | Дві третини відмов були серверним дефектом сканера глосарія (D54), а не якістю моделі. Після його виправлення 2 пачки дали карантин 5 і 4 із 50 (9%), у шар 37 і 38, у модерацію по 8. Класи 9 карантинних: 5 · назви справді немає (робота моделі), 3 · назва у відмінковій формі зі зміненою основою («Торговця з ліхтарем» для «Торговець з ліхтарем»), 1 · брудний запис глосарія із зайвою лапкою в назві | причина, приписана не тому шару | відкритий | лишилось 9% карантину трьох різних класів · кожен потребує свого рішення, див. BACKLOG |
| D54 | 2026-09-04 | Сервер відхиляв рядок із `glossary_violation` «не видно узгодженої назви X», коли X присутній у тексті ДОСЛІВНО · 60 рядків із 73 | `GlossaryMentionScanner::wordExpression()` різав основу разом із пунктуацією на кінці слова: «Еферії:» давало шаблон «Ефер» + літери, тобто двокрапка опинялась ПОСЕРЕДИНІ слова й шаблон не збігався ніколи (`GlossaryMentionScanner.php:194`) | шаблон, що ховає розділовий знак усередині слова | закритий | виправлено на сервері (коміт `c120d9b`, версія 4.7.8) + 4 regression у `GlossaryMentionScannerTest` і `test_a_name_present_verbatim_is_never_reported_as_missing`. Перевірено з нашого боку прогоном 2 пачок 2026-09-04: карантин 5 і 4 із 50 проти 15-25 до фіксу, і серед 9 карантинних рядків ЖОДНОЇ хибної відмови |
| D55 | 2026-09-04 | Екран `стан` показував живий прогін як зупинений: поле «Пачка: 20260904_110133» поруч із «останній виклик 08:02» · фантомний розрив у три години. Той самий зсув мали `./bdo model-run` і `./bdo judge --list` | Ідентифікатор пачки складає bash (`date +%Y%m%d_%H%M%S`, `cli/batch/batch-new.sh:49`) у поясі СИСТЕМИ (Europe/Kiev), а `at` у журналах · UTC, і PHP у наборі стоїть із `date.timezone=UTC` (`php -r 'echo ini_get("date.timezone");'` → `UTC`). Екрани друкували `at` підрядком (`substr($d["at"], 11, 8)`), тобто UTC як локальний час | вікно, що показує факт не в тій системі відліку | закритий | `tests/tui.sh` пункти 10-12: UTC 10:00 з `BDO_TZ=Europe/Kiev` мусить дати `12:00:00`, `grep` забороняє друк `at` підрядком у `bin/` і `cli/`, окремо перевірено `Bdo\Translate\Ui\Clock::ago()`. Екран додатково показує вік останнього руху пачки словами · «46 хв тому» замість віднімання в голові |
| D56 | 2026-09-04 | Карантин не може бути нулем, бо сервер відхиляє САМ запит у модерацію: квитанція `output/write_20260904_120628.json` → `layer=manual, mode=proposal, auto_approve=false, items 12, written 8, rejected 4`, код `glossary_violation`. Тобто суддя вже вирішив «до людини», ми надіслали рядок у чергу модерації, і чергу закрив сервер · рядку не лишилось жодного місця, крім файла | Ворота глосарію стоять на записі однаково для `mode=direct` і `mode=proposal`, хоча пропозиція · це ПИТАННЯ до людини, а не твердження про якість. Наш бік уже маршрутизує правильно (`lib/Pipeline/ChannelRouter.php:30` віддає в карантин лише текст без тексту), карантин наповнює `cli/write/write-translations.sh:197` з відмов API | єдиний вихід для дефектного рядка закритий тими ж ворітьми | закритий | прогін `20260904_143842` одразу після деплою: 50 рядків, 46 у шар, 4 у модерацію, **0 карантину** (`state/batches/20260904_143842_e9342220e94704ec/batch-summary.json`: `moderation_written: 4`, `moderation_rejected: 0`). Проба `POST /translations/validate` на тому самому рядку `6ce446b4…`, який до деплою відхилявся блокуюче, тепер дає `status: ok`. До деплою той самий фільтр давав 16 карантину з 50 (32%). Наш бік межу тримає далі: `tests/row-attempts.sh` (стеля спроб) і `tests/names-pass.sh` п. 2б (наказ лише для людської назви) |
| D57 | 2026-09-04 | Пачка перекладає 4 рядки, а перевіряє 49: журнал `state/batches/20260904_120154_*/journal.jsonl` → `translation-worker:4`, `translation-qa:49`, `translation-repair:11`, `translation-judge:10`. QA забирає 60% часу прогону (323 с із 537 с, 12 801 токен виходу), сам переклад · 4%. Ремонтник при цьому переписує 11 рядків (у пачці `110133` · 23); поіменно рядки не звірено, але 45 із 49 текстів пачки прийшли з памʼяті, тому переважна їх частина · уже перекладений і вже прийнятий текст | `dispatch_qa` бере `qa-subset.json`, зібраний із `rows.json` (усі 50 рядків пачки), а не з того, що насправді написала модель (`to-translate.json`, 4-5 рядків). Памʼять економить виклик перекладача й одразу віддає зекономлене QA | перевірка обсягом більша за роботу | закритий | `tests/qa-scope.sh`: пачка 50 рядків, 45 закриті памʼяттю · QA отримує рівно 5, рядки з памʼяті мають PASS від коду, після злиття 50 вердиктів із 50; `cli/quality/mechanical-split.sh --memory`, `dispatch_qa` у `run-drive.sh`. Памʼять із русизмом і далі дістає REJECT механікою |
| D58 | 2026-09-04 | Той самий рядок проходить повний конвеєр без межі: `state/quarantine.jsonl` · 597 записів на 84 унікальні identity, рядок `32c077fc69e5` пройшов конвеєр 21 раз, `063cf4a527a2` · 20. Квитанції `output/write_20260904_120149.json` і `write_20260904_120628.json` відхиляють ОДНІ Й ТІ САМІ хеші поспіль. Кожен такий прохід платить QA, ремонтом, суддею й записом | Вибірка `missing=machine&exclude_proposed=1` віддає рядок доти, доки він не записаний і не став пропозицією; рядок, якому відмовили в обох каналах, повертається в кожну наступну пачку. Локальний реєстр спроб `state/run-seen.json` існував і був прибраний 2026-08-26 (`cli/run/run-mode.sh:76-92`) за те, що ЗУПИНЯВ прогін, а не за те, що задача зникла | межа безпеки, знята разом із її дефектом | закритий | `tests/row-attempts.sh`: `Pipeline\RowAttempts` рахує спроби по identity, `fetch-rows.sh` не бере рядок після стелі `BDO_ROW_MAX_ATTEMPTS` (типово 2) і добирає сторінки, `run drive` віднімає виключені від «лишилось» і дає `goal_complete` з `waiting_human`; `./bdo quarantine --clear` обнуляє журнал разом із карантином |
| D59 | 2026-09-04 | Локальна модель третину відповіді витрачала на копіювання `identity_hash`: на QA з 49 рядків ~2 000 із 6 447 токенів виходу · 64 шістнадцяткові символи на кожен рядок, які треба прочитати й відтворити посимвольно. Це і вартість, і місце помилки (дубль identity 2026-08-22) | Ідентичність конвеєра (хеш) виходила за межу виклику моделі як є: payload, схема й відповідь несли повний хеш | службовий формат коштує дорожче за роботу | закритий | `tests/model-client.sh` п. 11-13: у запиті до моделі немає повного хеша, схема має `enum` `r1,r2`, відповідь повертає повні хеші; чужий `id` · відмова `unknown_id`; `BDO_ROW_ALIAS=0` повертає хеші. Реалізація · `Bdo\Translate\Model\RowAlias` у `cli/model/client.php`, промпти ролей кажуть `id` |
| D60 | 2026-09-04 | Прохід по назвах, доданий цього ж дня, змушував модель підставляти НЕ затверджену людиною назву: наказ «ужий «X» для «Y»» будувався з `details.glossary[].expected` без перевірки походження. Приклад із квитанції: рядок `ad739b68…` («…sea monsters have been spotted on the move») отримав mandatory-вимогу «Переміщення» для англійського `move` у прозі | У дампі `state/glossary-full.json` **131 391 запис із 136 022 має `ukrainian_layer: machine` при `severity: mandatory`**, `manual/mandatory` · лише 89. Тобто «затверджений відповідник» у 96,6% випадків є машинною здогадкою. Поле `ukrainian_layer` API віддає і в `/rows?fields=glossary`, і в `/rows/context` (перевірено на PROD), а наш код його викидав: `Row::glossary()` брав лише `canonical_source` і `ukrainian`, `worker-payload.sh` копіював фіксований перелік полів без нього | машинний вихід, поданий як закон | закритий | `tests/names-pass.sh` п. 2б: походження `machine` і ВІДСУТНЄ поле пропускають наказ і кажуть про це в stderr, `manual` · дає; `Row::glossaryLayers()` читає походження з того самого рядка, бо у відмові сервера цього поля немає |
| D61 | 2026-09-04 | Той самий клас лишається в лікуванні й у судді: `Response::rejections()` доклеює наказ «ужий «X» для «Y»» без походження назви, і саме за цим наказом `heal-plan.sh` дає рядку додаткову спробу, а суддя бачить його як `api_rejected` | Провенанс назви не доходить до жодного з цих місць: відмова API його не несе, а `rejections()` віддає готовий РЯДОК, у якому терміни вже склеєні | одна межа поставлена, клас закритий не весь | закритий | причину знято В ДЖЕРЕЛІ: сервер більше не видає `mandatory`-відмову на машинну назву, тому наказу «ужий «X»» із машинним походженням не виникає взагалі · у прогоні `20260904_143842` подій `names_pass` нуль, відмов `glossary_violation` на фінальній валідації нуль. Наш бік лишається з перевіркою походження (`Row::glossaryLayers()`, `tests/names-pass.sh` п. 2б), тому повернення серверної поведінки не зробить конвеєр знову небезпечним |
| D62 | 2026-09-04 | Власник відкривав «стан», бачив екран і ВИЛІТАВ у оболонку замість повернення в меню. Саме це було на його першому скріншоті: після розділу пачок одразу запит оболонки, без рядка «Enter · назад». Заміряно в справжньому PTY через tmux: `./bdo` завершувався з кодом 255 одразу після малювання екрана | `between` виходив `return 0` на стоп-рядку, а виклик обрізав вивід зовнішнім `head -12`; обидва закривали канал, доки `./bdo review` ще писав. Хвіст звіту пише `php`, він гине на EPIPE із кодом 255, `set -euo pipefail` у `project-review.sh` робить це кодом усього `./bdo review`, а `pipefail` у вікні · вироком конвеєра, після чого `set -e` убиває вікно | обрив каналу, прочитаний як відмова роботи | закритий | `tests/tui.sh` п. 2 (перевірено: на старому коді падає з названою причиною) і новий `tests/tui-live.sh` · справжній PTY у tmux, натискання 6/Enter/7/q, перевірка `[EXIT=0]`. Фікстуру `review` доведено до МЕЖІ: 15 рядків до стоп-рядка й хвіст від `php` після нього · зі старою фікстурою з одного `echo` тест був зеленим на зламаному вікні |
| D63 | 2026-09-04 | Власник десять хвилин дивився в німий екран: клієнт моделі просив відповідь одним куском (`stream: false`), тому робота ролі була невидимою до останньої секунди, а роздуми моделі не показувались узагалі й вважались несумісними з конвеєром | Обидва прапорці стояли жорстко `false` із виміру 2026-08-29, і той вимір не переглядався після оновлення Ollama. Перевимір 2026-09-04 (0.33.3, та сама модель, той самий constrained decoding): потік дав 721 чанк (703 з `thinking`, 17 з `content`), зібраний JSON валідний, `done_reason=stop`; думання дало валідний `content` разом із роздумами. Тобто заборона трималась на застарілому вимірі | вимір, який не переглянули після зміни оточення | закритий | `tests/model-client.sh` п. 12в-12ж: підроблений Ollama віддає NDJSON, перевірено збирання чанків, обрив без завершального чанка (`stream_incomplete`), «роздуми без `content`» як відмову, явне ввімкнення думання й запасний шлях `BDO_MODEL_STREAM=0`. Усі перевірки відповіді робляться на ЗІБРАНІЙ відповіді, тобто там само, де й раніше |
| D64 | 2026-09-04 | QA знижувала вердикт за невживання МАШИННОЇ назви глосарія й писала в поле `issue` міркування абзацами. Доказ із живого прогону `20260904_153814`: `REVIEW/minor · Недотримання glossary: 'Timing' перекладено як 'Час', хоча в контексті 'Perfect timing' це допустимо, але glossary вимагає…` · рядок знижено, і в тому ж реченні модель сама визнала candidate допустимим. На пачці 39 рядків це дало 14 `REVIEW/minor` | Payload не розрізняв походження відповідника: `Row::glossary()` віддавав один блок, і промпт називав його законом. У каталозі 131 391 запис із 136 022 · машинні, людських 172. Побачити це стало можливо лише після живого виводу генерації (D63): раніше вердикти лишались у файлі, який прибирався разом із пачкою | машинний вихід, поданий як закон | закритий | `tests/glossary-provenance.sh`: `Row::glossaryByLayer()` ділить блоки (відсутнє поле лишається на боці людини), payload воркера й QA віддає `glossary` окремо від `glossary_hint`, спільні `terms` несуть `ukrainian_layer`, а промпти чотирьох ролей трактують блоки різно · включно з дослівною забороною слів-маркерів міркування в `issue` |
| D65 | 2026-09-04 | `./bdo web --stop` друкував «Сервер зупинено», а сторінка далі відповідала старим токеном: після зупинки на порту лишалось 4 живі процеси (`lsof` показав `php … 127.0.0.1:7654 (LISTEN)` × 5, з них 4 після kill майстра) | `PHP_CLI_SERVER_WORKERS` створює воркери, які тримають ТОЙ САМИЙ слухаючий сокет; вбитий майстер сокета не звільняє, а зупинка перевіряла лише «процес зник» | успіх заявлено без доказу | закритий | `tests/web-server.sh` · після `--stop` запит на порт мусить дати `000`; фальсифіковано зняттям `set -m` · без окремої групи процесів зупинка вбиває власну групу (перевірено: впала сесія), тому додано заборону вбивати ГРУПУ, у якій живе сам скрипт |
| D66 | 2026-09-05 | `README.md` і `WORKFLOW.md` показували приклад `./bdo mode start патч 50 7` · тобто документація ВЧИЛА рівно тієї помилки, яку код уже закрив після D50 (ключі режимів англійські) | Після D50 виправили меню й додали `mode_key()`, але приклади в документації лишились із українським ключем: перевірка стосувалась коду, а не тексту | правило без перевірки в документації | закритий | `./bdo gate docs` · `check_rules` шукає `mode start (патч|ручний|пропозиції|покращення)` у всьому дереві, крім реєстру дефектів; фальсифіковано поверненням прикладу · gate падає |
| D67 | 2026-09-05 | У панелі генерації на сторінці замість тексту моделі йшов сирий NDJSON: `{"content":"ес"} {"content":"ен"} {"content":"ція"}` · власник бачив службові рядки журналу замість перекладу | `cli/model/client.php` пише потік по JSON-рядку на чанк, а сторінка показувала хвіст файла ЯК Є: складати текст не вмів ніхто | показ сирого журналу замість змісту | закритий | `tests/web-server.sh` · `/api/state` віддає зібраний текст, і в ньому не може бути підрядка `{"content"`; складання живе в `Web\Snapshot::assemble`, тому поверхні власного розбору не мають |
| D68 | 2026-09-05 | `./bdo web --stop` убив ЖИВУ пачку: сесія tmux зникла, пачка `20260905_023518` лишилась на `awaiting_worker`. Це протилежне обіцяному в §14 «сервер можна перезапустити · робота триває» | Зупинка вбивала ГРУПУ процесів (`kill -- -PGID`, введена проти D65), а прогін, запущений дією сторінки, успадковує групу сервера | надто широка ціль зупинки | закритий | `tests/web-server.sh` · групового вбивства в `cli/system/web.sh` бути не може (grep) плюс стара перевірка «після --stop порт вільний»; фальсифіковано поверненням `kill -- -` · тест падає |
| D69 | 2026-09-05 | Посеред живого прогону сторінка писала «драйвер не працює», кнопка старту лишалась активною, і другий прогін на тому самому стані ставав можливим · перевірка в `Runner` спиралась на ту саму хибну ознаку | Ознакою роботи був лише `drive.lock`, який існує ТІЛЬКИ під час кроку `run drive`; найдовше в пачці · виклик моделі, коли замка вже немає | ознака, що бреше на найдовшому кроці | закритий | `tests/web-actions.sh` · порожній стан не є прогоном, свіжий рух у `run-stream.log` є, десятихвилинна тиша знову ні; фальсифіковано в обидва боки. Ознака рахується ЗІ СВОГО `state/`, а не скануванням процесів · інакше чужий прогін з іншої теки показувався б як свій |
| D70 | 2026-09-05 | Сторінка мовчки показувала застарілий знімок: `/api/state` віддавав HTML-помилку PHP із кодом 200. Справжня причина (виняток у `Snapshot`) була не видна взагалі · її ховав САМ обробник | `lib/autoload.php` писав помилку у `STDERR`, а ця константа існує лише в CLI: під вбудованим сервером обробник падав із «Undefined constant STDERR» | обробник помилки ховає помилку | закритий | `tests/web-server.sh` · `/api/state` мусить бути валідним JSON без `Fatal error`; фальсифіковано штучним винятком у `Snapshot` при повернутому старому обробнику · тест падає |
| D12 | 2026-08-28 | Ефект економії (проходи, 30% `REVIEW -> ai_layer`, 3 виклики) не підтверджений на живих пачках | Зміни зроблено, прогону на новому шляху ще не було | заявлено без доказу | відкритий | немає перевірки · зняти з `children` у manifest і `state/judge-decisions.jsonl` |
