# Verification 6.3.9

Усі чотири sabotage виконувалися окремо й після кожного повністю відновлювалися.

## 1. Routing

Правило: wrapper передає керування у точний internal Kernel command.
Дослівний sabotage diff:
--- cli/batch/batch-dir.sh
+++ cli/batch/batch-dir.sh
@@
-if [ "${BDO_ORCHESTRATOR:-php}" != sh ]; then
-    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-    exec php "$SCRIPT_DIR/cli/bdo.php" batch-dir "$@"
-fi
 # Надрукувати теку поточної пачки; порожньо й код 1, якщо пачку не розпочато.
Команда: bash tests/cli-batch-heal-parity.sh
code=1 seconds=0.421698
FAIL: routing batch-dir: fake php відмовив (1): FAIL: неправильна форма PHP delegate
Відновлено: так.

## 2. Subset composition

Правило: output містить усі запитані hashes у source order.
Дослівний sabotage diff:
--- lib/Cli/Command/Batch/SubsetRowsCommand.php
+++ lib/Cli/Command/Batch/SubsetRowsCommand.php
@@
-                $subset[] = $row;
+                if ($hash !== "3333333333333333333333333333333333333333333333333333333333333333") {
+                    $subset[] = $row;
+                }
Команда: bash tests/cli-batch-heal-parity.sh
code=1 seconds=1.88538
FAIL: subset JSON не збігається
Відновлено: так.

## 3. Heal additional attempt

Правило: наказова API-підказка отримує рівно одну додаткову спробу.
Дослівний sabotage diff:
--- lib/Cli/Command/Heal/HealPlanCommand.php
+++ lib/Cli/Command/Heal/HealPlanCommand.php
@@
-            $limit = $actionable ? $maxAttempts + 1 : $maxAttempts;
+            $limit = $maxAttempts;
Команда: bash tests/heal-attempts.sh
code=1 seconds=0.777793
FAIL: рядок із наказовою підказкою НЕ отримав додаткової спроби
Відновлено: так.

## 4. Batch stable output

Правило: output зберігає точну кількість рядків.
Дослівний sabotage diff:
--- lib/Cli/Command/Batch/BatchNewCommand.php
+++ lib/Cli/Command/Batch/BatchNewCommand.php
@@
-$output->stdout(sprintf("Пачку розпочато: %s\\n  рядків: %d | ключ набору: %s\\n", $workspace->id(), count($rows), $rows->key()));
+$output->stdout(sprintf("Пачку розпочато: %s\\n  рядків: %d | ключ набору: %s\\n", $workspace->id(), count($rows) + 1, $rows->key()));
Команда: bash tests/cli-batch-heal-parity.sh
code=1 seconds=0.956828
  рядків: 3 | ключ набору: 3f063b7fa12ed2ad
  рядків: 4 | ключ набору: 3f063b7fa12ed2ad
FAIL: batch-new: stdout не збігається
Відновлено: так.
