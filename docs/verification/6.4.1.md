# Verification 6.4.1

Усі пʼять sabotage виконувалися окремо. Після кожного зміна була повністю
відновлена, а `bash tests/cli-batch-clean-parity.sh` знову дав code 0.

## 1. Routing

Правило: wrapper передає default route у `cli/bdo.php batch-clean`.

Дослівний sabotage diff:

```diff
--- cli/batch/batch-clean.sh
+++ cli/batch/batch-clean.sh
@@
-if [ "${BDO_ORCHESTRATOR:-php}" != sh ]; then
-    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-    exec php "$SCRIPT_DIR/cli/bdo.php" batch-clean "$@"
-fi
 # Прибрати все, що флоу вже не може використати.
```

Команда: `bash tests/cli-batch-clean-parity.sh`

```text
elapsed_seconds=1.685
exit_code=1
FAIL: routing batch-clean: marker відсутній
```

Відновлено: так; parity після restore: code 0.

## 2. Current batch

Правило: `current-batch` не видаляється і не входить у `--keep`.

Дослівний sabotage diff:

```diff
--- lib/Cli/Command/Batch/BatchCleanCommand.php
+++ lib/Cli/Command/Batch/BatchCleanCommand.php
@@
-                if ($currentId !== null && $name === $currentId) {
+                if (false && $currentId !== null && $name === $currentId) {
```

Команда: `bash tests/cli-batch-clean-parity.sh`

```text
elapsed_seconds=3.331
exit_code=1
FAIL: preview: stdout не збігається
```

Релевантна різниця: PHP надрукував `понад ліміт квитанцій, тека цілком:
20260101_000001_current`, тоді як shell надрукував `ПОТОЧНА, пропуск`.
Відновлено: так; parity після restore: code 0.

## 3. Повні 24 години

Правило: для `--days 0` файл віком 25 годин stale, файл віком 1 годину fresh.

Дослівний sabotage diff:

```diff
--- lib/Cli/Command/Batch/BatchCleanCommand.php
+++ lib/Cli/Command/Batch/BatchCleanCommand.php
@@
-        $age = max(0, time() - (int) @filemtime($path));
-
-        return intdiv($age, 86400) > $days;
+        return time() - (int) @filemtime($path) > $days * 86400;
```

Команда: `bash tests/cli-batch-clean-parity.sh`

```text
elapsed_seconds=3.360
exit_code=1
  дамп output: fresh-level2.json
FAIL: preview: stdout не збігається
```

Відновлено: так; parity після restore: code 0.

## 4. Preview

Правило: preview не змінює жодного файла.

Дослівний sabotage diff:

```diff
--- lib/Cli/Command/Batch/BatchCleanCommand.php
+++ lib/Cli/Command/Batch/BatchCleanCommand.php
@@
-                    if ($apply) {
+                    if (true) {
                         $this->deletePath($dir);
```

Команда: `bash tests/cli-batch-clean-parity.sh`

```text
elapsed_seconds=4.238
exit_code=1
FAIL: PHP preview змінив дерево
```

Відновлено: так; parity після restore: code 0.

## 5. Retained derived file

Правило: retained batch зберігає лише receipts і `drive.lock`; derived files
видаляються.

Дослівний sabotage diff:

```diff
--- lib/Cli/Command/Batch/BatchCleanCommand.php
+++ lib/Cli/Command/Batch/BatchCleanCommand.php
@@
-                    if (isset(self::RECEIPTS[$base]) || $base === 'drive.lock') {
+                    if (isset(self::RECEIPTS[$base]) || $base === 'drive.lock' || $base === 'rows.json') {
```

Команда: `bash tests/cli-batch-clean-parity.sh`

```text
elapsed_seconds=3.421
exit_code=1
  похідні файли (2) -> лишаємо квитанцію: 20260101_000003_retained (SIZE КБ)
FAIL: preview: stdout не збігається
```

Відновлено: так; parity після restore: code 0. Усі sabotage diff відсутні у
фінальному дереві.
