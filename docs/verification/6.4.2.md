# Verification 6.4.2

## Development HANDOFF contract

Новий guard у `scripts/agent-check.sh` перевіряє адресата технічного HANDOFF,
short-form skeleton, заборону власницьких заголовків і маршрут CI.

### Саботаж 1 · прибрано literal marker з HANDOFF.md

Дослівний sabotage diff:

```diff
--- /tmp/handoff-6.4.2-before-sabotage.md
+++ docs/ai-workflow/HANDOFF.md
@@ -1,8 +1,8 @@
 # Пакет звіту: що ВИКОНАВЕЦЬ повертає АРХІТЕКТОРУ
-АДРЕСАТ: АРХІТЕКТОР
+ЗВІТ
-Перший непорожній рядок будь-якої форми звіту · `АДРЕСАТ: АРХІТЕКТОР`.
+Перший непорожній рядок будь-якої форми звіту · адресат звіту.
 `## Підсумок`, `## Що зробив`, `## Що далі` належать лише відповіді
 АРХІТЕКТОРА ВЛАСНИКУ і ВИКОНАВЦЕМ не використовуються.
@@ -43,7 +43,7 @@
 Коротка форма має буквально таку структуру:
 ```text
-АДРЕСАТ: АРХІТЕКТОР
+ЗВІТ
 ## СТАН
 ## КОМІТ
 ## ЗМІНЕНІ ФАЙЛИ
@@ -59,7 +59,7 @@
 ## Повна форма
 ```text
-АДРЕСАТ: АРХІТЕКТОР
+ЗВІТ
 ## СТАН        (ГОТОВО | ЧАСТКОВО | ЗАБЛОКОВАНО · одне слово)
 ## КОМІТ       (хеш і версія; або «не комічено» з причиною)
 ## ЩО ЗРОБЛЕНО (до 5 рядків; зміна і навіщо)
```
Команда:

```text
set +e
start=$(date +%s%N)
./bdo gate docs > /tmp/docs-sabotage-1.out 2>&1
code=$?
end=$(date +%s%N)
printf 'code=%s\nelapsed_ms=%s\n' "$code" "$(( (end - start) / 1000000 ))"
```

Дослівний релевантний output:

```text
code=1
elapsed_ms=1156
== Development HANDOFF contract ==
FAIL: HANDOFF.md не містить literal marker адресата
```

Після `cp /tmp/handoff-6.4.2-before-sabotage.md docs/ai-workflow/HANDOFF.md`
повторний `./bdo gate docs` дав code 0 і `Agent gate passed: docs`.

### Саботаж 2 · повернуто «ВЛАСНИК пушить» у WORKFLOW.md

Дослівний sabotage diff:

```diff
--- /tmp/workflow-6.4.2-before-sabotage.md
+++ docs/ai-workflow/WORKFLOW.md
@@ -39,7 +39,7 @@
 3. **ВИКОНАВЕЦЬ → звіт.** Один блок за `HANDOFF.md`, навіть якщо робота вийшла
    тривіальною. Звіт адресований АРХІТЕКТОРУ, не власнику.
 4. **ВИКОНАВЕЦЬ → пуш.** Після зеленого гейта пушить сам і називає у HANDOFF
-   результат push. Результат CI він не вгадує і не зобов'язаний бачити; CI на
+   результат push. ВЛАСНИК пушить, а результат CI він не вгадує і не зобов'язаний бачити; CI на
    точному SHA перевіряє АРХІТЕКТОР.
```

Команда:

```text
set +e
start=$(date +%s%N)
./bdo gate docs > /tmp/docs-sabotage-2.out 2>&1
code=$?
end=$(date +%s%N)
printf 'code=%s\nelapsed_ms=%s\n' "$code" "$(( (end - start) / 1000000 ))"
```

Дослівний релевантний output:

```text
code=1
elapsed_ms=1186
== Development HANDOFF contract ==
FAIL: WORKFLOW.md містить застаріле твердження «ВЛАСНИК пушить»
```

Після `cp /tmp/workflow-6.4.2-before-sabotage.md docs/ai-workflow/WORKFLOW.md`
повторний `./bdo gate docs` дав code 0 і `Agent gate passed: docs`.
