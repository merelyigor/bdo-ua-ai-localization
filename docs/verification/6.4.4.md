# Verification 6.4.4

## Ephemeral `/tmp` references in verification evidence

`check_references()` пропускає backtick-reference, що починається рівно з
`/tmp/`; relative repository references і Markdown links перевіряються без змін.

### Positive proof · absolute ephemeral reference

Fixture: docs/verification/_tmp-reference-fixture.md, який містив рівно:

```text
Temporary evidence fixture: `/tmp/bdo-reference-sentinel-6.4.4.md`
```

Перед прогоном:

```text
test ! -e /tmp/bdo-reference-sentinel-6.4.4.md
sentinel_absent_code=0
```

`./bdo gate docs` дав:

```text
absolute_fixture_code=0
elapsed_ms=6856
Agent gate passed: docs
```

### Negative proof · relative repository reference

До прогону до fixture тимчасово додано (у fixture, не в цьому artifact):

```text
Negative repo reference: missing-bdo-reference-6.4.4.md (wrapped in backticks during the fixture run)
```

`./bdo gate docs` дав:

```text
relative_fixture_code=1
elapsed_ms=2127
FAIL: docs/verification/_tmp-reference-fixture.md посилається на відсутній missing-bdo-reference-6.4.4.md
```

Рядок negative reference відновлено; fixture після всіх прогонів видалено.

### Falsification · прибрано `/tmp/*` skip

Дослівний sabotage diff:

```diff
--- /tmp/agent-check-6.4.4-before-sabotage.sh
+++ scripts/agent-check.sh
@@ -305,9 +305,6 @@
     while IFS= read -r doc; do
         test -f "$doc" || continue
         while IFS= read -r ref; do
-            case "$ref" in
-                /tmp/*) continue ;;
-            esac
             case " $external " in *" $ref "*) continue ;; esac
             resolve_reference "$ref" "$(dirname "$doc")" \
                 || fail "$doc посилається на відсутній $ref"
```

Команда:

```text
set +e
start=$(date +%s%N)
./bdo gate docs > /tmp/docs-6.4.4-sabotage.out 2>&1
code=$?
end=$(date +%s%N)
printf 'sabotage_code=%s\nelapsed_ms=%s\n' "$code" "$(( (end - start) / 1000000 ))"
```

Дослівний релевантний output:

```text
sabotage_code=1
elapsed_ms=2093
FAIL: docs/verification/_tmp-reference-fixture.md посилається на відсутній /tmp/bdo-reference-sentinel-6.4.4.md
```

Після restore `scripts/agent-check.sh` повторний `./bdo gate docs` дав code 0 і
`Agent gate passed: docs`; тимчасовий fixture видалено, у commit він не входить.
