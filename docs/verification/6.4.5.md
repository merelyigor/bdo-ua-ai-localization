# Verification 6.4.5

## Безпека

Усі write-перевірки використовують `BDO_ENV=DEV`, `BDO_API_TARGET=legacy` і
локальний `127.0.0.1` HTTP stub. Реальний Agent API write не виконувався.

## Саботажі

### 1. Dispatcher write

Дослівна зміна з `cli/write/write-translations.sh`:

```diff
-if [ "${BDO_ORCHESTRATOR:-php}" != sh ]; then
-    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-    exec php "$SCRIPT_DIR/cli/bdo.php" write "$@"
-fi
```

Команда: `bash tests/cli-write-parity.sh`

Релевантний вивід:

```text
code=1
Ціль: PROD (https://bdo-ua.com.ua/api/agent/v1)
cli/write/write-translations.sh: line 50: 1: Потрібен файл items.json
FAIL: routing cli/write/write-translations.sh
```

Exit code: `1`; час: `0.424 s`. Dispatcher відновлено; фінальний parity-test: code `0`.

### 2. Non-write commit робить POST

Дослівна зміна в `lib/Cli/Command/Batch/BatchCommitCommand.php`:

```diff
-$doWrite = in_array('--write', $arguments, true);
+$doWrite = true;
```

Команда: `bash tests/cli-write-parity.sh`

Релевантний вивід:

```text
FAIL: commit-dry stdout
```

Exit code: `1`; час: `5.318 s`. Рядок відновлено; фінальний parity-test: code `0`, POST у 20-row dry-run: `0`.

### 3. Machine mapping змінено

Дослівна зміна в `lib/Api/TranslationWriter.php`:

```diff
-            'machine' => ['machine', 'direct', true],
+            'machine' => ['manual', 'proposal', true],
```

Команда: `bash tests/cli-write-parity.sh`

Релевантний вивід:

```text
--- /dev/fd/63
+++ /dev/fd/62
@@ -1,5 +1,5 @@
 Підготовлено 1 елементів
-Канал: machine (layer=machine, mode=direct, auto_approve=true)
+Канал: machine (layer=manual, mode=proposal, auto_approve=true)
FAIL: write-machine stdout
```

Exit code: `1`; час: `1.0 s`. Mapping відновлено; фінальний parity-test: code `0`.

### 4. Operational blockers ослаблено

Дослівна зміна в `lib/Cli/Command/Batch/BatchCommitCommand.php`:

```diff
-                if ($runTarget === '') {
+                if (false) {
                     $blocked = 'no_run:запусти cli/run/run-start.sh';
-                } elseif ($runTarget !== $environment['environment']) {
+                } elseif (false) {
                     $blocked = 'env_mismatch:прогін='.$runTarget.',команда='.$environment['environment'];
-                } elseif (count($pass) > $remaining) {
+                } elseif (false) {
                     $blocked = 'quota:'.$remaining.'_left';
```

Команда: `bash tests/cli-write-parity.sh`

Релевантний вивід:

```text
FAIL: no-run POST
```

Exit code: `1`; час: `4.0 s`. Усі три guard-умови відновлено; фінальний parity-test: code `0`.

### 5. Rejected item не записується у quarantine

Дослівна зміна в `lib/Api/TranslationWriter.php`:

```diff
-            if (in_array($row['status'] ?? '', ['ok', 'repaired', 'unchanged', 'skipped'], true)) {
+            if (in_array($row['status'] ?? '', ['ok', 'repaired', 'unchanged', 'skipped', 'rejected'], true)) {
```

Команда: `bash tests/cli-write-parity.sh`

Релевантний вивід:

```text
FAIL: rejected php quarantine.jsonl
```

Exit code: `1`; час: `5.0 s`. Умову відновлено; фінальний parity-test: code `0`.

### 6. Moderation dry робить POST

Дослівна зміна в `lib/Cli/Command/Write/ModerationCommand.php`:

```diff
-            if ($dry) {
+            if (false && $dry) {
```

Команда: `bash tests/cli-write-parity.sh`

Релевантний вивід:

```text
FAIL: moderation dry POST
```

Exit code: `1`; час: `4.0 s`. Умову відновлено; фінальний parity-test: code `0`.

## Фінальні перевірки

Усі write-тести виконані на localhost DEV stub; `POST /translations` у dry-run: `0`.
Реалізація не містить `exec`, `shell_exec`, `system`, `passthru`, `popen` або `proc_open`.
CI не перевірявся; його перевіряє АРХІТЕКТОР.
