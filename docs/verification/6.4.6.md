# Verification 6.4.6

Corrective до D119-D125 виконано лише на локальному DEV stub. Жодного write до
реального Agent API не виконувалось.

## Falsification evidence

1. D119 benchmark guard.

```diff
-        $candidateDirectory = realpath(dirname($candidateFile));
-        if ($candidateDirectory !== false
-            && basename($candidateDirectory) === 'benchmark'
-            && basename(dirname($candidateDirectory)) === 'output') {
-            $output->stderr("Це файл виміру (output/benchmark/), а не переклад. Записувати його не можна.\n");
-
-            return 1;
-        }
```

Command: `bash tests/cli-write-parity.sh`
Output: `FAIL: benchmark candidate прийнято`
Exit code: `1`; elapsed: `4.733 s`.
Restore: benchmark block restored; green rerun `bash tests/cli-write-parity.sh` -> `0`.

2. D120 production-root cleanup safety.

```diff
 # Перевірити права каналів поведінкою actual write command на DEV stub.
+# harmless sabotage marker: $ROOT/output
 set -euo pipefail
```

Command: `./bdo gate shell`
Output: `FAIL: write-тест має production-root cleanup: 3:# harmless sabotage marker: $ROOT/output`
Exit code: `1`; elapsed: `51.180 s`.
Restore: marker removed; `./bdo gate shell` -> `0`.

3. D122 quarantine padding.

```diff
-            $output->stdout(sprintf("Карантин: %s (%d рядків усього)\n", $quarantine, $quarantineLines));
+            $output->stdout(sprintf("Карантин: %s (%8d рядків усього)\n", $quarantine, $quarantineLines));
```

Command: `bash tests/cli-write-parity.sh`
Output: `FAIL: PHP quarantine count має бути без legacy padding`
Exit code: `1`; elapsed: `11.976 s`.
Restore: `%d` restored; green rerun `bash tests/cli-write-parity.sh` -> `0`.

4. D123 response metadata provenance.

```diff
-            'layer' => $meta['layer'] ?? null,
+            'layer' => $meta['layer'] ?? $result['layer'],
-            'mode' => $meta['mode'] ?? null,
+            'mode' => $meta['mode'] ?? $result['mode'],
-            'auto_approve' => $meta['auto_approve'] ?? null,
+            'auto_approve' => $meta['auto_approve'] ?? $result['auto_approve'],
```

Command: `bash tests/cli-write-parity.sh`
Output: `FAIL: missing layer не став null`
Exit code: `1`; elapsed: `8.468 s`.
Restore: response-only metadata restored; green rerun `bash tests/cli-write-parity.sh` -> `0`.

5. D124 required idempotency value.

```diff
-                if (! array_key_exists($index + 1, $arguments) || $arguments[$index + 1] === '') {
-                    $output->stderr("--idempotency-key потребує непорожній ключ\n");
-
-                    return 1;
-                }
```

Command: `bash tests/cli-write-parity.sh`
Output: `FAIL: required write-key missing PHP без назви option`
Exit code: `1`; elapsed: `5.396 s`.
Restore: required-value check restored; green rerun `bash tests/cli-write-parity.sh` -> `0`.

6. D125 current-workspace provenance.

```diff
         $currentWorkspace = Workspace::current($stateDir);
-        $receipt = $currentWorkspace?->path('candidate.json.session.json') ?? '';
+        $receipt = dirname($candidateFile).'/candidate.json.session.json';
```

Command: `bash tests/cli-write-parity.sh`
Output: `FAIL: current-write request log` with `wrong-provider/wrong-model` in the PHP request and `fixture-provider/fixture-model` in shell.
Exit code: `1`; elapsed: `8.863 s`.
Restore: current-workspace receipt restored; green rerun `bash tests/cli-write-parity.sh` -> `0`.

7. D121 actual mechanical routing.

```diff
-                $mechanical = $hasText ? Defects::inTranslation($row, (string) $text) : [];
+                $mechanical = [];
```

Command: `bash tests/mechanical-final-check.sh`
Output: `FAIL: actual batch-commit не лишив clean row у PASS` and actual output `До запису: 2 | у модерацію: 0`.
Exit code: `1`; elapsed: `0.213 s`.
Restore: `Defects::inTranslation` call restored; green rerun `bash tests/mechanical-final-check.sh` -> `0`.

8. Static subprocess guard.

```diff
     ) {
+        exec('true');
     }
```

Command: `./bdo gate shell`
Output: `FAIL: заборонений subprocess у lib/Api/TranslationWriter.php:34:        exec('true');`
Exit code: `1`; elapsed: `50.749 s`.
Restore: sabotage line removed; green rerun `./bdo gate shell` -> `0`.

9. D121 quota branch.

```diff
-                } elseif (count($pass) > $remaining) {
+                } elseif (false) {
```

The fixture was first corrected from `run-target=DEV` to `run-target=local`, so
the test reaches quota rather than env mismatch.
Command: `bash tests/cli-write-parity.sh`
Output: `FAIL: quota POST`
Exit code: `1`; elapsed: `12.379 s`.
Restore: quota comparison restored; green rerun `bash tests/cli-write-parity.sh` -> `0`.

## Green verification

The final green run covered the isolated localhost DEV harness. No production
root cleanup occurred, and no real API write was sent.
