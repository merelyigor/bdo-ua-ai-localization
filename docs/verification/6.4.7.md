# Verification 6.4.7

Corrective D126-D128 перевірено лише на локальному DEV stub. Жодного PROD write
не виконувалось.

## D126 · mixed operational blockers

Exact sabotage diff у `lib/Cli/Command/Batch/BatchCommitCommand.php`:

```diff
                 }
                 $pass = [];
-                $moderation = [];
             }
```

Command: `bash tests/cli-write-parity.sh`

Relevant output:

```text
-До запису: 0 | у модерацію: 0 (з них нерозпізнані назви: 0) | у карантин (збої): 2 | квота: 100
+До запису: 0 | у модерацію: 1 (з них нерозпізнані назви: 0) | у карантин (збої): 2 | квота: 100
+Записано: 1  Пропущено: 0  Відкинуто: 0
+У МОДЕРАЦІЮ: 1 рядків (черга пропозицій, не карантин)
FAIL: mixed-no-run stdout
```

Exit code: `1`; elapsed: `15.66s`.
Restore: `$moderation = [];` restored. Green rerun: `bash tests/cli-write-parity.sh` -> code `0`.

## D127 · config worker provenance

Exact sabotage diff in `lib/Cli/Command/Batch/BatchCommitCommand.php`:

```diff
-            $route = $model === '' ? 'unknown/agent' : 'ollama/'.$model;
+            $route = $model === '' ? 'unknown/agent' : $model;
```

Command: `bash tests/cli-write-parity.sh`

Relevant output:

```text
-..."provider":"ollama","model":"qwen3.6:35b-a3b-mtp-q4_K_M"...
+..."provider":"qwen3.6:35b-a3b-mtp-q4_K_M","model":"qwen3.6:35b-a3b-mtp-q4_K_M"...
FAIL: config-fallback request log
```

Exit code: `1`; elapsed: `12.42s`.
Restore: `ollama/` prefix restored. Green rerun: `bash tests/cli-write-parity.sh` -> code `0`.

## D128 · production-root safety guard

Exact sabotage diff in `tests/cli-write-parity.sh`:

```diff
 ROOT="$HARNESS"
+rm -f "$SOURCE_ROOT/output/__d128_probe__"
 HARNESS_OUTPUT="$ROOT/${HARNESS_OUTPUT_DIR:-output}"
```

Command: `./bdo gate shell`

Relevant output:

```text
== Write-test safety ==
FAIL: write-тест має production-root output/state access: 18:rm -f "$SOURCE_ROOT/output/__d128_probe__"
```

Exit code: `1`; elapsed: `50.22s`.
The write parity test did not run and the probe was not created. Restore removed
the line. Green rerun: `./bdo gate shell` -> code `0`.

## Green verification

`bash tests/cli-write-parity.sh` -> `0`.
`./bdo gate shell` -> `0`.
