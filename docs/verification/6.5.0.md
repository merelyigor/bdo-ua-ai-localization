# Verification 6.5.0

Пакет перевірено на ізольованих synthetic DEV env/state fixtures. Мережевих
запитів і PROD write не виконувалось.

## Falsification evidence

1. Run-target safety.

```diff
-                if (in_array($batchState, ['none', 'verified', 'failed_terminal'], true)) {
+                if (in_array($batchState, ['none', 'verified', 'failed_terminal', 'awaiting_worker'], true)) {
```

Command: `time bash tests/cli-run-foundation-parity.sh`
Output: `FAIL: foreign-awaiting_worker: stdout не збігається`
Exit code: `1`; elapsed: `7.305 s`.
Restore: `awaiting_worker` прибрано зі списку terminal states; green rerun
`bash tests/cli-run-foundation-parity.sh` -> code `0` за `6.489 s`.

2. RunSpec concrete patch/domain.

```diff
-            $preset['filter'] = RunSpec::filterFor($mode, $patch, $domain);
+            $preset['filter'] = RunSpec::preset($mode)['filter'];
```

Command: `time bash tests/cli-run-foundation-parity.sh`
Output: `FAIL: status-snapshot-domain: stdout не збігається`
Exit code: `1`; elapsed: `1.019 s`.
Restore: `RunSpec::filterFor($mode, $patch, $domain)` restored; green rerun
`bash tests/cli-run-foundation-parity.sh` -> code `0` за `6.448 s`.

3. Wrapper routing.

```diff
-    exec php "$SCRIPT_DIR/cli/bdo.php" run-spec "$@"
+    exec php "$SCRIPT_DIR/cli/bdo.php" run-start "$@"
```

Command: `time bash tests/cli-run-foundation-parity.sh`
Output: `unexpected php route: <repo>/cli/bdo.php run-start`
followed by `FAIL: run-spec routing proof`.
Exit code: `1`; elapsed: `0.335 s`.
Restore: `run-spec` internal route restored; green rerun
`bash tests/cli-run-foundation-parity.sh` -> code `0` за `6.419 s`.

4. No-subprocess guard.

```diff
     public function execute(array $arguments, Output $output): int
     {
+        exec('true');
         $stateDir = $this->stateDir();
```

Command: `/usr/bin/time -p ./bdo gate shell`
Output: `FAIL: заборонений subprocess у lib/Cli/Command/Run/RunStartCommand.php:\n23:        exec('true');`
Exit code: `1`; elapsed: `149.59 s`.
Restore: `exec('true')` removed; green rerun `/usr/bin/time -p ./bdo gate shell`
-> code `0`, `real 157.05`.

## Green verification

`./bdo gate shell` -> code `0`, `real 165.02` before final sabotage cycle.
`./bdo gate docs` -> code `0`, `real 6.79`.
`./bdo gate full` -> code `0`, `real 211.79`.
`./bdo inspect lib/Cli/Command/Run` -> code `0`, `Помилок: 0, попереджень: 0`.
`./bdo api` -> code `0`, `real 0.68`, read-only smoke.
`bash tests/run-target-env.sh` -> code `0`.
`bash tests/api-target-switch.sh` -> code `0`.
`bash tests/run-resume.sh` -> code `0`.

All sabotage changes were restored before the final checks. No real API write,
translation flow, production state or output state was touched.
