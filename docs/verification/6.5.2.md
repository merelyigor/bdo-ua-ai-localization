# Verification 6.5.2

## D131: bounded timestamp normalization

`tests/cli-run-foundation-parity.sh` тепер приймає часові bounds лише парою.
Без bounds `run-started-at` копіюється побайтово; з bounds raw value спочатку
перевіряється як integer milliseconds у вікні запуску, і лише після цього
нормалізується до `TIMESTAMP`. Self-test зберігає різницю `111`/`222` без
bounds та перевіряє `run-target`, manifest/state і error reason.

## Sabotage 1: broad normalizer

Exact sabotage diff:

```diff
-        elif [ "$relative" = run-started-at ] && [ -n "$before" ] && [ -n "$after" ]; then
+        elif [ "$relative" = run-started-at ]; then
```

Command:

```text
time bash tests/cli-run-foundation-parity.sh
```

Relevant output:

```text
FAIL: normalizer: raw timestamps без bounds порівнялись однаково
bash tests/cli-run-foundation-parity.sh  1.39s user 0.63s system 83% cpu 2.407 total
```

Exit code: `1`; elapsed: `2.407s`.

Restore: bounded condition restored exactly. Green rerun:
`bash tests/cli-run-foundation-parity.sh` → code `0`, `10.782s`.

## Sabotage 2: blocked-state mutation

Exact sabotage diff:

```diff
                     $output->stderr("Поверни BDO_ENV до '{$current}' та заверши пачку або попроси власника явно відмовитися від неї.\n");

+                    file_put_contents($stateDir.'/run-started-at', "sabotage\n");
                     return 1;
```

Command:

```text
time bash tests/cli-run-foundation-parity.sh
```

Relevant output:

```text
FAIL: foreign-awaiting_worker: run-started-at state differs
bash tests/cli-run-foundation-parity.sh  6.39s user 2.94s system 90% cpu 10.275 total
```

Exit code: `1`; elapsed: `10.275s`.

Restore: the temporary `file_put_contents` line was removed; production
`RunStartCommand.php` has no final diff against baseline. Green rerun:
`bash tests/cli-run-foundation-parity.sh` → code `0`, `10.782s`.

## Final evidence

The `foreign-awaiting_worker` case compares code `1`, `ЗАБЛОКОВАНО`, exact
`run-target`, `run-started-at`, `run-batches.json`, `run-seen.json`,
`current-batch`, and `batches/foreign/manifest.json` without timestamp
normalization. Successful starts pass their measured before/after bounds to the
snapshot. No production PHP behavior was changed.
