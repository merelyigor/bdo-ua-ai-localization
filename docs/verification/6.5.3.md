# Verification 6.5.3

## Scope

`run-mode.sh` default routing, in-process fetch with `resultPath()`, resume,
zero-row completion, budget/goal state I/O and the rollback shell were tested
against an isolated local DEV HTTP stub. No model call, POST or PROD write was
performed.

## Falsification

Each sabotage was applied only temporarily, run against the local harness, and
restored before the next check. The green rerun after every restore was:
`bash tests/cli-run-mode-parity.sh` -> code 0.

### 1. Dispatcher routing

Exact temporary diff:

```diff
-if [ "${BDO_ORCHESTRATOR:-php}" != sh ]; then
-    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
-    exec php "$SCRIPT_DIR/cli/bdo.php" run-mode "$@"
-fi
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output: `FAIL: run-mode routing proof`

Exit code: 1; elapsed: 304 ms. Restore: dispatcher restored; parity rerun code 0.

### 2. Structured fetch result path

Exact temporary diff:

```diff
-$rowsPath = $fetch->resultPath();
+$rowsPath = $root.'/output/rows_99999999_999999.json';
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output: `FAIL: fresh-patch: stdout differs` (the stale-sentinel path
prevented the selected result from being returned).

Exit code: 1; elapsed: 4031 ms. Restore: `resultPath()` restored; parity rerun code 0.

### 3. Resume early return

Exact temporary diff:

```diff
-if (! in_array($state, ['verified', 'failed_terminal'], true)) {
+if (false && ! in_array($state, ['verified', 'failed_terminal'], true)) {
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output: `FAIL: resume: stdout differs`; the PHP side selected a new
batch instead of returning the existing batch.

Exit code: 1; elapsed: 12880 ms. Restore: resume condition restored; parity rerun code 0.

### 4. Budget boundary

Exact temporary diff:

```diff
-if ($count >= $max) {
+if ($count > $max) {
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output: `FAIL: budget PHP did not stop`

Exit code: 1; elapsed: 18083 ms. Restore: `>=` restored; parity rerun code 0.

### 5. Corrupt budget state fallback

Exact temporary diff:

```diff
-$output->stderr('Пошкоджений файл стану: '.$path."\n");
-return null;
+$run = [];
```

The same fallback was applied to the non-array branch for the sabotage only.
Command: `bash tests/cli-run-mode-parity.sh`

Relevant output: `FAIL: corrupt: stdout differs`; shell reported
`Пошкоджений файл стану: .../run-batches.json`, while PHP created selected work.

Exit code: 1; elapsed: 20294 ms. Restore: both fail-closed branches restored;
parity rerun code 0.

### 6. Failed goal write ignored

Exact temporary diff:

```diff
         if (@file_put_contents($goalPath, $goal, LOCK_EX) === false) {
             $output->stderr('Не вдалося записати файл стану: '.$goalPath."\n");
-            return 1;
         }
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output: `FAIL: goal-failure: stdout differs`; PHP reported the goal
path but proceeded to selected batch creation.

Exit code: 1; elapsed: 21969 ms. Restore: `return 1` restored; parity rerun code 0.
