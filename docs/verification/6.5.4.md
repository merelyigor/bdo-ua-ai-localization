# Verification 6.5.4

## D133 proof

`run_side()` now shifts the five service arguments and forwards the complete
remaining argv to both rollback shell and direct PHP. The differential covers
actual `patch 20 3 premium_shop` and `improve 20 2 quest` arguments, exact
localhost GET request logs, goal/manifest/query/channel/memory-layer fields,
existing-spec resume immutability, foreign-target blocking before HTTP,
size-15 failure before HTTP/budget/goal/batch, and exhausted-budget fetch
ordering. The healthy parity rerun returned code 0.

## Sabotage 1 · patch/domain loss

Temporary diff:

```diff
         $patch = (string) ($arguments[2] ?? 'active');
         $domain = (string) ($arguments[3] ?? '');
+        $patch = 'active';
+        $domain = '';
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
FAIL: numeric: stdout differs
```

Exit code: `1`; elapsed: `14.498s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore1_code=0`).

## Sabotage 2 · resume overwrite

Temporary diff:

```diff
-                if ((string) ($manifest['mode'] ?? '') === '') {
+                if (true) {
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
FAIL: resume: stdout differs
```

Exit code: `1`; elapsed: `18.748s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore2_code=0`).

## Sabotage 3 · target-lock bypass

Temporary diff:

```diff
-        if ($startCode !== 0) {
+        if (false && $startCode !== 0) {
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
ЗАБЛОКОВАНО: незавершена пачка має state='awaiting_worker' і ціль 'hub-local',
а BDO_ENV вимагає 'local'. Не вгадуй --end і не перемикай середовище.
FAIL: target-mismatch: stdout differs
```

Exit code: `1`; elapsed: `19.905s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore3b_code=0`).

## Sabotage 4 · invalid-size bypass

Temporary diff:

```diff
-            $fetchCode = $fetch->execute([$size, $query], $fetchOutput);
+            $fetchCode = $fetch->execute(['50', $query], $fetchOutput);
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
-{"method":"GET","uri":"/rows?limit=20&include_total=1&fields=classification,tokens,constraints,glossary,reference,patch&patch=3&missing=machine&domain=premium_shop"}
+{"method":"GET","uri":"/rows?limit=50&include_total=1&fields=classification,tokens,constraints,glossary,reference,patch&patch=3&missing=machine&domain=premium_shop"}
FAIL: numeric: HTTP request logs differ
```

Exit code: `1`; elapsed: `16.064s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore4_code=0`).

All sabotage used isolated localhost fixtures only; no API write, model call,
translation flow, or production state was used.
