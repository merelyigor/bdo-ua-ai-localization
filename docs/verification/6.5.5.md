# Verification 6.5.5

## D134 proof

The previous directory fixture remains the read/decode failure branch. A
separate `budget-write-failure` fixture creates a regular readable valid JSON
`run-batches.json` with `scope=local:patch:active:` and `batches=0`, keeps the
parent state directory writable, applies mode `0444`, and mechanically checks
`test -r`, successful JSON decoding, and `test ! -w` before either command.
Both shell and PHP then fetch, fail at the budget write, name the write path,
leave the budget file byte-identical, and create neither goal nor batch.

The size sabotage is conditional on `$size === '15'`; the numeric size-20
patch/domain case remains green and the failure is reached at `size15`.

## Sabotage 1 · PHP budget-write guard

Temporary diff:

```diff
         if (@file_put_contents($path, $newRun, LOCK_EX) === false) {
-            $output->stderr('Не вдалося записати файл стану: '.$path."\n");
-            return null;
+            return true;
         }
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
FAIL: budget-write-failure: stdout differs
```

The PHP side continued and emitted a selected batch while shell reported the
write failure. Exit code: `1`; elapsed: `32.480s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore1_code=0`).

## Sabotage 2 · rollback budget-write guard

Temporary diff:

```diff
 if (@file_put_contents($f,json_encode($run,JSON_UNESCAPED_UNICODE|JSON_THROW_ON_ERROR),LOCK_EX)===false) {
-    fwrite(STDERR,"Не вдалося записати файл стану: $f\n"); exit(1);
+    // sabotage: ignore failed budget write
 }
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
FAIL: budget-write-failure: stdout differs
```

The shell side continued and emitted a selected batch while PHP reported the
write failure. Exit code: `1`; elapsed: `36.823s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore2_code=0`).

## Sabotage 3 · narrow size15 bypass

Temporary diff:

```diff
-            $fetchCode = $fetch->execute([$size, $query], $fetchOutput);
+            $fetchCode = $fetch->execute([$size === '15' ? '50' : $size, $query], $fetchOutput);
```

Command: `bash tests/cli-run-mode-parity.sh`

Relevant output:

```text
FAIL: size15: stdout differs
```

The earlier numeric `patch 20 3 premium_shop` case passed; the PHP size15
side made a request and reached selected state instead of `fetch_failed`.
Exit code: `1`; elapsed: `22.176s`.

The sabotage was restored exactly. Green rerun: `bash tests/cli-run-mode-parity.sh` -> `0` (`restore3_code=0`).

All scenarios used isolated temporary state/output and the localhost GET-only
stub. No POST, model call, translation flow, or PROD write was performed.
