# Verification 6.5.6

Усі HTTP-сценарії нижче працювали лише через synthetic DEV stub на
`http://127.0.0.1:<port>`. Реальний Agent API і PROD write не використовувалися.

## Базова перевірка

`bash tests/cli-run-drive-parity.sh` -> code 0

```text
run-drive parity: OK · isolated no-batch, routing, selected child, D136-D138 fail-closed and no-Unix proofs
```

Parity охоплює isolated no-batch, child, dry-run без translation POST, localhost
normal write з GET `/me` та POST `/translations`, structured validate artifact,
corrupt retry/goal/summary state, goal-status retry, live roles/timed steps і
direct PHP no-Unix paths для no-batch, child та verified completion.

## Falsification

### 1. Wrapper routing

Тимчасовий diff:

```diff
-    exec php "$SCRIPT_DIR/cli/bdo.php" run-drive "$@"
+    exec php "$SCRIPT_DIR/cli/bdo.php" run-drive-sabotaged "$@"
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
wrong route
```

Code `92`, elapsed `0.766s`; після restore parity -> code `0`.

### 2. Structured validate artifact

Тимчасовий diff:

```diff
-        $this->resultPath = $out;
+        $this->resultPath = null;
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
FAIL: validate resultPath() повернув stale або порожній artifact
```

Code `1`, elapsed `9.744s`; після restore parity -> code `0`.

### 3. Dry-run safety

Тимчасовий diff:

```diff
-        if (getenv('BDO_DRY_RUN') !== '1') $args[] = '--write';
+        if (true) $args[] = '--write';
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
FAIL: shell/PHP output mismatch: .../sh-dry.out .../php-dry.out
-..."target_written":0...
+..."target_written":1...
```

Code `1`, elapsed `4.984s`; dry-run assertion перехопила фактичний write
результат, після restore parity -> code `0`.

### 4. D136 retry fail-open

Тимчасовий PHP diff:

```diff
-            if ($state === null) { $this->emit($output, false, (string) $this->manifest()['state'], ['kind' => 'blocked', 'reason' => 'retry_state_unavailable', 'path' => $path]); return null; }
+            if ($state === null) { $state = []; }
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
FAIL: retry-corrupt/php code=0
```

Code `1`, elapsed `9.222s`; restore і parity -> code `0`.

Тимчасовий rollback-shell diff:

```diff
-            if (!is_readable($f)) { fwrite(STDERR, "Не вдалося прочитати файл стану: $f\n"); exit(2); }
+            if (!is_readable($f)) { $a=[]; }
-            catch (Throwable) { fwrite(STDERR, "Пошкоджений файл стану: $f\n"); exit(2); }
-            if (!is_array($a)) { fwrite(STDERR, "Пошкоджений файл стану: $f\n"); exit(2); }
+            catch (Throwable) { $a=[]; }
+            if (!is_array($a)) { $a=[]; }
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
FAIL: retry-corrupt/sh code=0
```

Code `1`, elapsed `10.060s`; rollback block restore і parity -> code `0`.

### 5. D137 unknown goal status

Тимчасовий PHP diff:

```diff
-            $this->emit($output, false, 'verified', ['kind' => 'retry', 'reason' => 'goal_status_unavailable', 'path' => $goalPath]); return null;
+            return $envelope;
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
FAIL: goal-unavailable/php code=0
```

Code `1`, elapsed `8.455s`; restore і parity -> code `0`. Goal transport/meta
failure therefore не може стати `complete`.

### 6. D138 summary persistence

Тимчасовий PHP diff:

```diff
-        if (! $this->writeJsonAtomic($runFile, $run)) return $this->completionError($output, 'run_summary_unavailable', $runFile);
+        $this->writeJsonAtomic($runFile, $run);
```

Команда: `bash tests/cli-run-drive-parity.sh`

```text
FAIL: summary-failure/php code=0
```

Code `1`, elapsed `10.113s`; restore і parity -> code `0`. Failed summary
persistence не може emit success/prune.

### 7. Live role source

Тимчасовий PHP diff:

```diff
-        'translation-repair', 'translation-judge', 'translation-names',
+        'translation-repair', 'translation-judge', 'translation-names', 'translation-unknown',
```

Команда: `bash scripts/agent-check.sh agents`

```text
FAIL: рушій кличе ролі, яких немає в config/roles.json: translation-unknown
```

Code `1`, elapsed `0.500s`; restore, а наступний shell gate перевірив live
allowlist -> code `0`.

### 8. Native timed-step source

Тимчасовий PHP diff:

```diff
-    private const TIMED_STEPS = ['validate.early', 'validate.final', 'commit'];
+    private const TIMED_STEPS = ['validate.early', 'validate.final'];
```

Команда: `bash tests/step-times.sh`

```text
FAIL: RunDriveCommand::timedSteps() не публікує «commit»
```

Code `1`, elapsed `0.771s`; restore, `bash tests/step-times.sh` -> code `0`.

Усі production sabotage відновлені; фінальний production diff містить тільки
дозволений перенос і D136-D138 hardening.
