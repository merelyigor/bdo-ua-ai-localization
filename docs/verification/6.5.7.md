# Verification 6.5.7

## Scope

Corrective proof for D139-D147 on isolated temporary repositories. No real
`state/**`, `output/**`, `.env`, model flow, live Agent API, or PROD write was
used. The differential test uses only a localhost DEV stub.

## Green differential

Command:

```text
bash tests/cli-run-drive-parity.sh
```

Final rerun after all restores: code `0`.

```text
run-drive parity: OK · isolated state-machine differential, D136-D147 branches, local write, and no-Unix proofs
```

The test covers malformed terminology retry/quarantine and exhaustion (D139),
ready term-note submit/effective threshold (D140), online concepts refresh and
offline no-request (D141), recovery guards and optional helper fallback (D142-D143),
post-write completion failure (D144), old/new output prune (D145), exhausted
invalid-QA quarantine (D146), and the three D147 goal outcomes: offline unknown
`complete`, explicit zero `goal_complete`, and online unknown `retry`. Local write
evidence is restricted to the test stub at `127.0.0.1`.

## Sabotage evidence

Each sabotage was applied temporarily with `apply_patch`, run against the
isolated parity test, and restored before the next sabotage. Elapsed values are
the shell `SECONDS` measurement printed with each run.

### D139 malformed terminology response

Temporary diff:

```diff
-                @rename($proposalFile, $this->workspace->path('term-proposals.invalid.'.time().'.json'));
-                $retry = $this->retryExceeded('awaiting_terminology:'.$chunk, $output);
-                if ($retry === null) return 1;
-                if ($retry !== 'exhausted') return $this->child($output, 'awaiting_terminology', 'translation-terminology', $this->workspace->path('terminology-payload.json'), $proposalFile);
+                @unlink($proposalFile);
+                $this->write($this->workspace->path('terminology-chunk'), ($chunk + 1)."\n");
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: term-invalid/php advanced chunk`
Code: `1`  · elapsed: `10s`
Restore: complete; final parity code `0`.

### D140 ready term-note submit

Temporary diff:

```diff
-                try {
-                    ApiEnvironment::load($this->root);
-                    $submit = $this->capture(new TermNotesSubmitCommand(), []);
-                    $output->stderr($submit['stdout'].$submit['stderr']);
-                } catch (\Throwable) {
-                    // Term-note submission is an optional side effect; the batch remains runnable.
-                }
+                @unlink($termResponse);
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: term-notes request differential differs` with the PHP log missing
`GET /glossary/terms` and `POST /glossary/proposals`
Code: `1`  · elapsed: `16s`
Restore: complete; final parity code `0`.

### D141 concepts refresh

Temporary diff:

```diff
-                $this->capture(new GlossaryConceptsCommand(), []);
+                if (false) $this->capture(new GlossaryConceptsCommand(), []);
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: concepts request differential differs`; the PHP request log
missed `GET /glossary/concepts`
Code: `1`  · elapsed: `16s`
Restore: complete; final parity code `0`.

### D142 memory-covered resume

Temporary diff:

```diff
-        if (is_file($toTranslate) && $this->jsonRows($toTranslate) === 0 && is_file($memoryCandidate) && filesize($memoryCandidate) > 0) {
+        if (false && is_file($toTranslate) && $this->jsonRows($toTranslate) === 0 && is_file($memoryCandidate) && filesize($memoryCandidate) > 0) {
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: memory-covered/php dispatched worker`
Code: `1`  · elapsed: `24s`
Restore: complete; final parity code `0`.

### D143 optional helper becomes gating

Temporary diffs:

```diff
-                $this->capture(new TermNotesQueueCommand(), [$this->workspace->path('terms.json'), $selected]);
+                $queueResult = getenv('BDO_TERM_NOTES_QUEUE_SABOTAGE') === '1'
+                    ? ['code' => 1]
+                    : $this->capture(new TermNotesQueueCommand(), [$this->workspace->path('terms.json'), $selected]);
+                if ($queueResult['code'] !== 0) return $this->emit($output, false, (string) $this->manifest()['state'], ['kind' => 'retry', 'reason' => 'term_notes_queue_failed']);
```

The temporary test-only seam exported `BDO_TERM_NOTES_QUEUE_SABOTAGE=1` for
the optional-queue case and was removed during restore.

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: optional-queue/php code=1`
Code: `1`  · elapsed: `27s`
Restore: complete; final parity code `0`.

### D144 post-write completion failure

Temporary diff:

```diff
         envelope="$(completion)"
-        completion_kind="$(php -r '...')"
-        if [ "$completion_kind" = blocked ] || [ "$completion_kind" = retry ]; then
-            emit 0 verified "$envelope"
-            exit 1
-        fi
         prune_verified_batch; auto_clean
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: post-write-summary/sh code=0`
Code: `1`  · elapsed: `30s`
Restore: complete; final parity code `0`.

### D145 output prune

Temporary diff:

```diff
-        $outputDir = getenv('BDO_STATE_DIR') ? dirname($this->stateDir).'/output' : $this->root.'/output';
-        $manifest = $this->workspace->path('manifest.json');
-        $manifestTime = is_file($manifest) ? (int) filemtime($manifest) : PHP_INT_MAX;
-        foreach (array_merge(glob($outputDir.'/rows_*.json') ?: [], glob($outputDir.'/validate_*.json') ?: []) as $path) {
-            if (is_file($path) && (int) filemtime($path) <= $manifestTime) @unlink($path);
-        }
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: output-prune/php kept old dump`
Code: `1`  · elapsed: `33s`
Restore: complete; final parity code `0`.

### D146 exhausted invalid-QA quarantine

Temporary diff:

```diff
-                    @rename($verdicts, $this->workspace->path('verdicts.invalid.'.time().'.json'));
+                    if (false) @rename($verdicts, $this->workspace->path('verdicts.invalid.'.time().'.json'));
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: invalid-qa-exhausted/php kept canonical verdicts`
Code: `1`  · elapsed: `32s`
Restore: complete; final parity code `0`.

### D147 offline goal envelope

Temporary diff:

```diff
 goal_remaining() {
-    if [ "${BDO_PIPELINE_OFFLINE:-0}" = 1 ]; then printf '__OFFLINE_UNKNOWN__'; return 0; fi
+    if [ "${BDO_PIPELINE_OFFLINE:-0}" = 1 ]; then printf '0'; return 0; fi
 }
```

Command: `bash tests/cli-run-drive-parity.sh`
Output: `FAIL: offline-goal/sh did not preserve complete`
Code: `1`  · elapsed: `34s`
Restore: complete; `bash tests/cli-run-drive-parity.sh` → code `0`; `bash tests/batch-summary.sh` → code `0`.

## Final safety

No live API or PROD write was used. Production and rollback changes are limited
to the requested D139-D147 implementation, with shell changes limited to the
D144 completion-kind guard and D147 offline-unknown sentinel handling.
