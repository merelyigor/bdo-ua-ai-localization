# Verification 6.5.8

## Scope

All drive scenarios ran in the isolated harness of tests/cli-run-drive-parity.sh.
The HTTP stub was DEV-only at 127.0.0.1; no live Agent API or PROD write was
used. cli/run/run-drive.sh and tests/batch-summary.sh remained frozen.

## D148

The invalid nonempty names-fixes.json case ran both rollback and PHP. Before the
retry child, both sides quarantined the invalid response, rebuilt
current-response-schema.json from names-subset.json, and emitted
translation-names. The proof checked the identity enum in the actual schema
artifact, not only the envelope.

The zero-byte qa-payload.json case rebuilt a nonempty QA payload before
redispatch. The zero-byte term-proposals.json case reached terminology only
when the real row had a terminology gap. Legacy improve without memory_layers
selected manual downstream; a pre-set BDO_MEMORY_LAYERS=all remained effective
downstream.

## D149

Submit-response and queue-threshold fixtures are separate. With raw queue >= MIN
but eligible queue < MIN because of missing identity/snapshot and already-proposed
names, neither side created translation-glossary or term-notes-payload.json.
With eligible queue exactly MIN, both sides created translation-glossary and its
payload.

## D150

Each optional helper was injected only into the temporary harness copy. The
injected shell helper wrote a marker and exited 7; the injected PHP helper wrote
the same marker and threw RuntimeException. The marker was checked before the
fallback assertion. All cases completed non-gating with shell/PHP parity:

concepts, memory-apply, term-notes-describe, term-notes-queue,
terminology-payload, check-russianisms, judge-payload, and names-payload.

## Targeted sabotage

Each source was restored immediately after its red run; later sabotage runs also
traversed the earlier restored case, and the final full parity rerun was green.

| case | temporary diff | command/result | restore |
|---|---|---|---|
| D148 schema retry | removed BuildSchemaCommand(names-subset.json) from invalid names-fixes retry | bash tests/cli-run-drive-parity.sh -> code 1; FAIL: names-invalid/php schema was not rebuilt (elapsed about 45 s) | restored the exact call; subsequent matrix continued |
| D142 zero-byte QA | changed !is_file(...) || filesize(...) === 0 to !is_file(...) | same command -> code 1; FAIL: qa-zero-payload/php payload stayed empty (elapsed about 26 s) | restored the filesize check |
| D142 zero-byte terminology | changed !is_file(termProposals) || filesize(...) === 0 to !is_file(...) | same command -> code 1; FAIL: term-proposals-zero/php skipped terminology (elapsed about 25 s) | restored the nonempty-file condition |
| D142 improve fallback | removed layers = manual | same command -> code 1; FAIL: improve-memory-fallback/php did not use manual memory (elapsed about 25 s) | restored the fallback assignment |
| D142 env preservation | changed the BDO_MEMORY_LAYERS === false guard to unconditional assignment | same command -> code 1; FAIL: memory-layer-preserved/php overwrote external layer (elapsed about 25 s) | restored the external-layer guard |
| D149 raw count | replaced eligibleTermNotes(...) with raw terms count | same command -> code 1; FAIL: term-threshold-negative/php evaluated raw queue threshold (elapsed about 55 s) | restored eligibleTermNotes(...) |
| D150 concepts | changed optionalCapture(GlossaryConceptsCommand) to capture | same command -> code 1; FAIL: d150-concepts/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 memory-apply | changed optionalCapture(MemoryApplyCommand) to capture | same command -> code 1; FAIL: d150-memory-apply/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 term-notes-describe | changed optionalCapture(TermNotesDescribeCommand) to capture | same command -> code 1; FAIL: d150-term-notes-describe/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 term-notes-queue | changed optionalCapture(TermNotesQueueCommand) to capture | same command -> code 1; FAIL: d150-term-notes-queue/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 terminology-payload | changed optionalCapture(TerminologyPayloadCommand) to capture | same command -> code 1; FAIL: d150-terminology-payload/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 russianisms | changed optionalCapture(CheckRussianismsCommand) to capture | same command -> code 1; FAIL: d150-check-russianisms/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 judge-payload | changed optionalCapture(JudgePayloadCommand) to capture | same command -> code 1; FAIL: d150-judge-payload/php code=1 (elapsed about 45 s) | restored optionalCapture |
| D150 names-payload | changed optionalCapture(NamesPayloadCommand) to capture | same command -> code 1; FAIL: d150-names-payload/php code=1 (elapsed about 45 s) | restored optionalCapture |

## Green verification

bash tests/cli-run-drive-parity.sh -> code 0
bash tests/terminology-chunks.sh -> code 0
bash tests/drive-memory-layers.sh -> code 0
bash tests/names-pass.sh -> code 0
bash tests/judge-flow.sh -> code 0
bash tests/batch-summary.sh -> code 0
bash tests/driver-loop.sh -> code 0

Final parity output:
run-drive parity: OK · isolated state-machine differential, D136-D147 branches, local write, and no-Unix proofs.
