# Verification 6.5.9

## Baseline

- branch: main
- HEAD: 98d7834085d3e58a9e661d7e4a3b2488dedf4306
- origin/main: 98d7834085d3e58a9e661d7e4a3b2488dedf4306
- version: 6.5.8
- working tree was clean before the Architect patch.

## PHPStan green-first preflight

Official release asset:
https://github.com/phpstan/phpstan/releases/download/2.2.13/phpstan.phar

Command:
php /tmp/phpstan-2.2.13.phar --version

Result: code 0, PHPStan - PHP Static Analysis Tool 2.2.13.

Read-only baseline command, before phpstan.neon and CI implementation:
php /tmp/phpstan-2.2.13.phar analyse -l 0 --no-progress lib

Result: code 0, elapsed 5.45 s, [OK] No errors.

Final configuration command:
php /tmp/phpstan-2.2.13.phar analyse -c phpstan.neon --no-progress

Result: code 0, elapsed 5.27 s, [OK] No errors.

phpstan.neon contains only level 0 and path lib; no baseline or ignoreErrors is
used. The workflow adds an independent hard-failing PHPStan level 0 job on
ubuntu-24.04, PHP 8.5, with shivammathur/setup-php@v2 and phpstan:2.2.13.

ПРАВИЛО: required detector is admitted only after a zero-result baseline run.
САБОТАЖ: an undefined method in lib/Cli/Kernel.php must make the same config
command fail and must identify that file.

## Falsification

Temporary exact diff:
@@ -72,6 +72,7 @@ final class Kernel
     public function run(array $arguments): int
     {
+        $this->phpstanSixFiveNineUndefinedMethod();
         $name = (string) ($arguments[0] ?? 'help');

Command:
php /tmp/phpstan-2.2.13.phar analyse -c phpstan.neon --no-progress

Relevant output:
Line   Cli/Kernel.php
75     Call to an undefined method
       Bdo\Translate\Cli\Kernel::phpstanSixFiveNineUndefinedMethod().
       method.notFound
[ERROR] Found 1 error

Result: code 1, elapsed 1.75 s.

Restore removed the temporary call byte-for-byte. git diff -- lib/Cli/Kernel.php
was empty after restore. The same config command then returned code 0 in 1.63 s
with [OK] No errors.

## Process evidence

D151 remains open in docs/plans/DEFECTS.md; this package does not add the
missing shell/PHP BDO_DRY_RUN=1 commit-report.txt identity proof.
D152 is closed only by the actual independent hard-failing PHPStan job and the
successful undefined-method falsification above.

git diff --check: code 0.
./bdo gate full: code 0, Agent gate passed: full.
No live API smoke is required because this package changes neither API code nor
the API contract.
