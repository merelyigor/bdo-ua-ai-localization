# Verification 6.6.0

Baseline: `0115e8761beb8f202c4e970c35521849e4f28472`, `main`, version `6.5.9`.
Final scope: `cli/batch/batch-commit.sh`, `lib/Cli/Command/Run/RunDriveCommand.php`,
`tests/cli-run-drive-parity.sh` and this evidence file plus the two Architect
process patches.

## D151 raw 20-row proof

`tests/cli-run-drive-parity.sh` generated and mechanically validated exactly
20 unique lowercase 64-hex identities, 20 source rows, 20 candidates and 20
PASS verdicts before execution. Both sides used the same absolute temporary
`BDO_STATE_DIR`; shell ran first, then the mutated state was removed and the
snapshot restored to that identical path. `diff -qr` of snapshot and restored
input was empty.

Both runs used `BDO_DRY_RUN=1`, `BDO_AUTO_CLEAN=0` and the synthetic DEV
localhost stub. Shell exit code: 0. PHP exit code: 0. Each raw
`commit-report.txt` was nonempty and contained `Пачка: 20 рядків`. Shell and
PHP reports were 495 bytes with SHA-256
`f3bbdc4d6ebad566c8731516e4340238f35e66d16b84ae05b1c211f20d29dd27`.
Raw `cmp -s`: code 0. Each request log contained the allowed quota GET only;
POST shell: 0, POST PHP: 0.

## D153 implementation

Native commit uses a dedicated shared temporary stream for stdout and stderr;
generic separated `capture()` remains unchanged. Rollback strips `wc -l`
padding to decimal digits before printing the same numeric count.

## Falsification

ПРАВИЛО: commit report identity requires merged write chronology.
САБОТАЖ: native commit temporarily used separated capture and concatenated stdout then stderr.

Temporary diff:
~~~diff
- $report = $this->captureMerged(new BatchCommitCommand(), $args);
+ $report = $this->capture(new BatchCommitCommand(), $args);
- $this->write($this->workspace->path('commit-report.txt'), $report['output']);
+ $this->write($this->workspace->path('commit-report.txt'), $report['stdout'].$report['stderr']);
~~~
Command: `bash tests/cli-run-drive-parity.sh`.
Elapsed: 7 s. Exit code: 1. Relevant failure: `FAIL: D151 raw commit-report.txt
byte identity mismatch`; diagnostic relocated `Ціль: DEV` from the first line to
the last line. Native-only sabotage did not alter envelope/counts or API.
Restore reapplied merged capture; targeted rerun exit code: 0.

ПРАВИЛО: quarantine count is canonical plain decimal across shell/PHP.
САБОТАЖ: rollback temporarily added one leading space before the count.

Temporary diff:
~~~diff
- echo "Карантин: $QUARANTINE ($QUARANTINE_COUNT рядків усього)"
+ echo "Карантин: $QUARANTINE ( $QUARANTINE_COUNT рядків усього)"
~~~
Command: `bash tests/cli-run-drive-parity.sh`.
Elapsed: 7 s. Exit code: 1. Relevant failure: `FAIL: D151 raw
commit-report.txt byte identity mismatch`; diagnostic named the `Карантин:`
line and showed the extra leading space. Restore reapplied the canonical
decimal line; targeted rerun exit code: 0.

## Checks

`bash tests/cli-run-drive-parity.sh` -> 0.
`bash tests/cli-write-parity.sh` -> 0.
`bash tests/batch-summary.sh` -> 0.
PHPStan 2.2.13 `php /tmp/phpstan-2.2.13.phar analyse -c phpstan.neon --no-progress` -> 0.
`git diff --check` -> 0.
`./bdo gate full` -> 0; Agent gate passed: full.

No live API, POST, PROD write or translation flow was executed.
