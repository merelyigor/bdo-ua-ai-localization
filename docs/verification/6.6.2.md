# Verification 6.6.2

Baseline: branch `preflight/windows-native-6.6.1`, SHA `4e6efcc31369d8cfb800b40de4c6ed345f3df290`, version `6.6.1`.
`origin/main` remains `b11cf9aea57b839bfb2368145bb1a8ad134441df`.

## D154 correction

Exact Windows run №30 (`34661567183`) found that the 6.6.1 smoke used PHP
directory plus `System32` in `PATH`; `bash` was therefore still executable and
the guard failed before positive cases. The correction keeps the absolute
`php.exe` resolved before isolation and sets the sanitized PATH exactly to the
directory containing that executable. `SystemRoot` is not cleared or changed.

The smoke checks `Get-Command bash.exe -CommandType Application` and requires a
null result before running any positive case. It does not install Bash, Git
Bash, WSL, or any other shell. Synthetic DEV environment data remains under
`RUNNER_TEMP`; inherited BDO base/key/environment variables remain cleared.

## Runtime contracts

Absolute PHP runs `cli/bdo.php help` and requires code 0, empty stderr, the root
help header, and the registry line. It then runs
`cli/bdo.php run-spec status patch active` and requires code 0, JSON
`ok=true`, `mode=patch`, `patch=active`, `domain=null`, plus the synthetic DEV
target announcement on stderr. This pure-PHP status path requires no HTTP.

The same sanitized PATH runs `cli/bdo.php env`; its nonzero exit is the negative
control because `EnvCommand` depends on unavailable `bash`. No error text is
used as the contract.

Production PHP diff versus `4e6efcc` is zero. Workflow diff versus `4e6efcc` is
zero; only the existing Windows job executes this corrective smoke.

Local `git diff --check` -> 0.
Local `./bdo gate full` -> 0.
PowerShell Windows runtime was not claimed locally on this non-Windows host.

Windows CI result: pending Architect review on corrective preflight branch
