# Verification 6.6.3

Baseline: `main@da14382cd93f6848fe91d8635661f49442bd9370`, version `6.6.2`.
Package version: `6.6.3`. The migration source is `cli/run/run-loop.sh`; the
native target is `RunLoopCommand` routed by `Kernel` and reached by the default
wrapper. `BDO_ORCHESTRATOR=sh` remains the frozen rollback path.

## Native loop differential

`tests/driver-loop.sh` uses independent temporary harnesses for the same
envelope fixtures. The shell side runs the frozen body; the PHP side runs the
real `RunLoopCommand`, while sandbox `cli/bdo.php` supplies only synthetic
`run-drive`/`run-mode` envelopes and the file-based model client supplies
responses. Payload and response values remain file paths. The differential
covers child role order, continue-run fixed argv, human stdout before JSON,
no-envelope, blocked, retry spin limit, signals 130/143, child failure,
unknown kind/mode/domain, `--once`, `--batches 1`, reporter success/failure and
the completeStep regression. Timestamp normalization is limited to the
leading `[HH:MM:SS]` transcript marker.

Result: `bash tests/driver-loop.sh` -> code 0.

`RunLoopCommand` uses fixed argv arrays with one `proc_open` helper. Drive
stdout/stderr stay separate; model stderr is forwarded while the child runs;
`next.command` is never executed. `drive`, `model.<role>` and `mode.start` are
measured with direct `StepTimes`; `step-report.sh` is the only temporary,
fail-soft visibility bridge until Stage7.

## D135

`./bdo help fetch` now finds the post-dispatcher documentation header, while
legacy `./bdo help platform` remains visible. `bash tests/command-registry.sh`
-> code 0. `bash tests/step-report.sh` and `bash tests/step-times.sh` -> code 0.

Rule: detailed help must read documentation after migrated dispatchers and from
legacy scripts. Sabotage: restoring the old `sed -n '2,/^[^#]/p'` boundary.
Temporary diff:

```diff
-    awk '
-        NR == 1 && /^#!/ { next }
-        !started && /^#/ { started=1; print; next }
-        started && /^#/ { print; next }
-        started { exit }
-    ' "$1" | sed -e 's/^# //' -e 's/^#//'
+    sed -n '2,/^[^#]/p' "$1" | sed -e '/^[^#]/d' -e 's/^# //' -e 's/^#//'
```

Command: `bash tests/command-registry.sh`. Relevant output:
`FAIL: migrated help fetch втратив documentation header`; code `1`; elapsed
`0s`. `bdo` was restored with `apply_patch`; the targeted test returned code 0.

## Loop falsification

Rule: validated machine mode keys and spin/batch safety remain code-owned.
Sabotage: temporarily add `патч` to the native `in_array` allowlist.

```diff
-        if (! in_array($mode, ['patch', 'manual', 'proposal', 'improve'], true)) {
+        if (! in_array($mode, ['patch', 'manual', 'proposal', 'improve', 'патч'], true)) {
```

Command: `bash tests/driver-loop.sh`. Relevant output:
`FAIL: php output misses: невідомий режим`; code `1`; elapsed `3s`.
`lib/Cli/Command/Run/RunLoopCommand.php` was restored byte-for-byte; the targeted differential
returned code 0.

## Process falsification

Rule: the native loop may use only one `proc_open` helper and it must receive an
argv array, not a shell command string. Sabotage: temporarily replace
`proc_open($command, ...)` with `proc_open(implode(' ', $command), ...)`.

```diff
-        $process = proc_open($command, $descriptors, $pipes, $this->root, $environment);
+        $process = proc_open(implode(' ', $command), $descriptors, $pipes, $this->root, $environment);
```

Command: `./bdo gate shell`. Relevant output:
`FAIL: RunLoopCommand proc_open не отримує argv-array helper`; code `1`;
elapsed `254s`. The native source was restored before the next check; targeted
`bash tests/driver-loop.sh` and the final gate returned code 0.

## Checks

`php -l lib/Cli/Command/Run/RunLoopCommand.php` -> code 0.
`php -l lib/Cli/Kernel.php` -> code 0.
`bash tests/driver-loop.sh` -> code 0.
`bash tests/command-registry.sh` -> code 0.
`bash tests/step-times.sh` -> code 0.
`./bdo api` -> code 0; read-only only, no key or URL recorded here.
`./bdo gate full` -> code 0.

`run-stop.sh` and production `step-report.sh` were not migrated. No live
translation, PROD write, WSL2 work, API contract change, or parallel
similarity/model-benchmark topic was performed. Exact main CI run №33
(`34671276926`) on `fca119fda9e63bd2ed845165062758db25141063` completed
success in all four jobs. Architect verdict: `NEEDS CORRECTION` because
D155/D156 show that the Windows job did not execute the migrated `run-loop`,
whose `proc_open()` pipe loop uses unsupported Windows `stream_select()`
semantics.
