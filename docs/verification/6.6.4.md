# Verification 6.6.4

Baseline: `main@fca119fda9e63bd2ed845165062758db25141063`; version `6.6.3`.

D155 was identified from the green-but-insufficient Windows CI run №33
(`34671276926`): the migrated loop used `stream_select()` on `proc_open()`
pipe descriptors. D156 was the missing runtime coverage: the Windows smoke
ran only `help`, `run-spec status`, and the shell-dependent `env` negative
control.

The corrective keeps one fixed argv-array `proc_open($command, ...)`.
POSIX keeps the existing non-blocking pipe and `stream_select()` path. On
Windows, stdout and stderr use two unique temporary regular files from the
descriptor specification; the loop polls `proc_get_status()` and reads only
new stderr bytes for live forwarding. Final output is read from both files,
and temporary files are removed in `finally`. Capture creation/read failures
remain named failures.

The Windows smoke now uses the real absolute PHP binary and a .NET
`ProcessStartInfo`/`WaitForExit(10000)` boundary for
`php cli/bdo.php run-loop --once` against a newly created empty
`BDO_STATE_DIR`. It requires completion before the hard timeout, exit code 1,
and `no_current_batch` on stderr. A timeout must fail with the literal
`run-loop timed out on Windows native process capture`. The smoke still proves
`bash.exe` application absence before the positive native-PHP cases; `env`
remains the same-path nonzero negative control.

No Windows runtime was claimed locally on this macOS host. No workflow,
shell rollback, API, model, write, Stage7, or production `step-report.sh`
change was made.

Local `php -l lib/Cli/Command/Run/RunLoopCommand.php` -> code 0.
Local `bash tests/driver-loop.sh` -> code 0.
Local `bash tests/command-registry.sh` -> code 0.
Local `git diff --check` -> code 0.
Local `./bdo gate full` -> code 0.

CI result: exact main run №34 (`34672774867`) on `d10922140008d49947441e4b2517afb797641b71` completed `success`. All four jobs passed: `./bdo gate full`, `cli-api-fetch × 5`, `PHPStan level 0`, and `Windows native PHP smoke`. Architect verdict: `ACCEPTED`; Stage6 is accepted.
